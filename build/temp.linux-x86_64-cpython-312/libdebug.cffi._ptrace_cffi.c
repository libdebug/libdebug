#define _CFFI_

/* We try to define Py_LIMITED_API before including Python.h.

   Mess: we can only define it if Py_DEBUG, Py_TRACE_REFS and
   Py_REF_DEBUG are not defined.  This is a best-effort approximation:
   we can learn about Py_DEBUG from pyconfig.h, but it is unclear if
   the same works for the other two macros.  Py_DEBUG implies them,
   but not the other way around.

   The implementation is messy (issue #350): on Windows, with _MSC_VER,
   we have to define Py_LIMITED_API even before including pyconfig.h.
   In that case, we guess what pyconfig.h will do to the macros above,
   and check our guess after the #include.

   Note that on Windows, with CPython 3.x, you need >= 3.5 and virtualenv
   version >= 16.0.0.  With older versions of either, you don't get a
   copy of PYTHON3.DLL in the virtualenv.  We can't check the version of
   CPython *before* we even include pyconfig.h.  ffi.set_source() puts
   a ``#define _CFFI_NO_LIMITED_API'' at the start of this file if it is
   running on Windows < 3.5, as an attempt at fixing it, but that's
   arguably wrong because it may not be the target version of Python.
   Still better than nothing I guess.  As another workaround, you can
   remove the definition of Py_LIMITED_API here.

   See also 'py_limited_api' in cffi/setuptools_ext.py.
*/
#if !defined(_CFFI_USE_EMBEDDING) && !defined(Py_LIMITED_API)
#  ifdef _MSC_VER
#    if !defined(_DEBUG) && !defined(Py_DEBUG) && !defined(Py_TRACE_REFS) && !defined(Py_REF_DEBUG) && !defined(_CFFI_NO_LIMITED_API)
#      define Py_LIMITED_API
#    endif
#    include <pyconfig.h>
     /* sanity-check: Py_LIMITED_API will cause crashes if any of these
        are also defined.  Normally, the Python file PC/pyconfig.h does not
        cause any of these to be defined, with the exception that _DEBUG
        causes Py_DEBUG.  Double-check that. */
#    ifdef Py_LIMITED_API
#      if defined(Py_DEBUG)
#        error "pyconfig.h unexpectedly defines Py_DEBUG, but Py_LIMITED_API is set"
#      endif
#      if defined(Py_TRACE_REFS)
#        error "pyconfig.h unexpectedly defines Py_TRACE_REFS, but Py_LIMITED_API is set"
#      endif
#      if defined(Py_REF_DEBUG)
#        error "pyconfig.h unexpectedly defines Py_REF_DEBUG, but Py_LIMITED_API is set"
#      endif
#    endif
#  else
#    include <pyconfig.h>
#    if !defined(Py_DEBUG) && !defined(Py_TRACE_REFS) && !defined(Py_REF_DEBUG) && !defined(_CFFI_NO_LIMITED_API)
#      define Py_LIMITED_API
#    endif
#  endif
#endif

#include <Python.h>
#ifdef __cplusplus
extern "C" {
#endif
#include <stddef.h>

/* This part is from file 'cffi/parse_c_type.h'.  It is copied at the
   beginning of C sources generated by CFFI's ffi.set_source(). */

typedef void *_cffi_opcode_t;

#define _CFFI_OP(opcode, arg)   (_cffi_opcode_t)(opcode | (((uintptr_t)(arg)) << 8))
#define _CFFI_GETOP(cffi_opcode)    ((unsigned char)(uintptr_t)cffi_opcode)
#define _CFFI_GETARG(cffi_opcode)   (((intptr_t)cffi_opcode) >> 8)

#define _CFFI_OP_PRIMITIVE       1
#define _CFFI_OP_POINTER         3
#define _CFFI_OP_ARRAY           5
#define _CFFI_OP_OPEN_ARRAY      7
#define _CFFI_OP_STRUCT_UNION    9
#define _CFFI_OP_ENUM           11
#define _CFFI_OP_FUNCTION       13
#define _CFFI_OP_FUNCTION_END   15
#define _CFFI_OP_NOOP           17
#define _CFFI_OP_BITFIELD       19
#define _CFFI_OP_TYPENAME       21
#define _CFFI_OP_CPYTHON_BLTN_V 23   // varargs
#define _CFFI_OP_CPYTHON_BLTN_N 25   // noargs
#define _CFFI_OP_CPYTHON_BLTN_O 27   // O  (i.e. a single arg)
#define _CFFI_OP_CONSTANT       29
#define _CFFI_OP_CONSTANT_INT   31
#define _CFFI_OP_GLOBAL_VAR     33
#define _CFFI_OP_DLOPEN_FUNC    35
#define _CFFI_OP_DLOPEN_CONST   37
#define _CFFI_OP_GLOBAL_VAR_F   39
#define _CFFI_OP_EXTERN_PYTHON  41

#define _CFFI_PRIM_VOID          0
#define _CFFI_PRIM_BOOL          1
#define _CFFI_PRIM_CHAR          2
#define _CFFI_PRIM_SCHAR         3
#define _CFFI_PRIM_UCHAR         4
#define _CFFI_PRIM_SHORT         5
#define _CFFI_PRIM_USHORT        6
#define _CFFI_PRIM_INT           7
#define _CFFI_PRIM_UINT          8
#define _CFFI_PRIM_LONG          9
#define _CFFI_PRIM_ULONG        10
#define _CFFI_PRIM_LONGLONG     11
#define _CFFI_PRIM_ULONGLONG    12
#define _CFFI_PRIM_FLOAT        13
#define _CFFI_PRIM_DOUBLE       14
#define _CFFI_PRIM_LONGDOUBLE   15

#define _CFFI_PRIM_WCHAR        16
#define _CFFI_PRIM_INT8         17
#define _CFFI_PRIM_UINT8        18
#define _CFFI_PRIM_INT16        19
#define _CFFI_PRIM_UINT16       20
#define _CFFI_PRIM_INT32        21
#define _CFFI_PRIM_UINT32       22
#define _CFFI_PRIM_INT64        23
#define _CFFI_PRIM_UINT64       24
#define _CFFI_PRIM_INTPTR       25
#define _CFFI_PRIM_UINTPTR      26
#define _CFFI_PRIM_PTRDIFF      27
#define _CFFI_PRIM_SIZE         28
#define _CFFI_PRIM_SSIZE        29
#define _CFFI_PRIM_INT_LEAST8   30
#define _CFFI_PRIM_UINT_LEAST8  31
#define _CFFI_PRIM_INT_LEAST16  32
#define _CFFI_PRIM_UINT_LEAST16 33
#define _CFFI_PRIM_INT_LEAST32  34
#define _CFFI_PRIM_UINT_LEAST32 35
#define _CFFI_PRIM_INT_LEAST64  36
#define _CFFI_PRIM_UINT_LEAST64 37
#define _CFFI_PRIM_INT_FAST8    38
#define _CFFI_PRIM_UINT_FAST8   39
#define _CFFI_PRIM_INT_FAST16   40
#define _CFFI_PRIM_UINT_FAST16  41
#define _CFFI_PRIM_INT_FAST32   42
#define _CFFI_PRIM_UINT_FAST32  43
#define _CFFI_PRIM_INT_FAST64   44
#define _CFFI_PRIM_UINT_FAST64  45
#define _CFFI_PRIM_INTMAX       46
#define _CFFI_PRIM_UINTMAX      47
#define _CFFI_PRIM_FLOATCOMPLEX 48
#define _CFFI_PRIM_DOUBLECOMPLEX 49
#define _CFFI_PRIM_CHAR16       50
#define _CFFI_PRIM_CHAR32       51

#define _CFFI__NUM_PRIM         52
#define _CFFI__UNKNOWN_PRIM           (-1)
#define _CFFI__UNKNOWN_FLOAT_PRIM     (-2)
#define _CFFI__UNKNOWN_LONG_DOUBLE    (-3)

#define _CFFI__IO_FILE_STRUCT         (-1)


struct _cffi_global_s {
    const char *name;
    void *address;
    _cffi_opcode_t type_op;
    void *size_or_direct_fn;  // OP_GLOBAL_VAR: size, or 0 if unknown
                              // OP_CPYTHON_BLTN_*: addr of direct function
};

struct _cffi_getconst_s {
    unsigned long long value;
    const struct _cffi_type_context_s *ctx;
    int gindex;
};

struct _cffi_struct_union_s {
    const char *name;
    int type_index;          // -> _cffi_types, on a OP_STRUCT_UNION
    int flags;               // _CFFI_F_* flags below
    size_t size;
    int alignment;
    int first_field_index;   // -> _cffi_fields array
    int num_fields;
};
#define _CFFI_F_UNION         0x01   // is a union, not a struct
#define _CFFI_F_CHECK_FIELDS  0x02   // complain if fields are not in the
                                     // "standard layout" or if some are missing
#define _CFFI_F_PACKED        0x04   // for CHECK_FIELDS, assume a packed struct
#define _CFFI_F_EXTERNAL      0x08   // in some other ffi.include()
#define _CFFI_F_OPAQUE        0x10   // opaque

struct _cffi_field_s {
    const char *name;
    size_t field_offset;
    size_t field_size;
    _cffi_opcode_t field_type_op;
};

struct _cffi_enum_s {
    const char *name;
    int type_index;          // -> _cffi_types, on a OP_ENUM
    int type_prim;           // _CFFI_PRIM_xxx
    const char *enumerators; // comma-delimited string
};

struct _cffi_typename_s {
    const char *name;
    int type_index;   /* if opaque, points to a possibly artificial
                         OP_STRUCT which is itself opaque */
};

struct _cffi_type_context_s {
    _cffi_opcode_t *types;
    const struct _cffi_global_s *globals;
    const struct _cffi_field_s *fields;
    const struct _cffi_struct_union_s *struct_unions;
    const struct _cffi_enum_s *enums;
    const struct _cffi_typename_s *typenames;
    int num_globals;
    int num_struct_unions;
    int num_enums;
    int num_typenames;
    const char *const *includes;
    int num_types;
    int flags;      /* future extension */
};

struct _cffi_parse_info_s {
    const struct _cffi_type_context_s *ctx;
    _cffi_opcode_t *output;
    unsigned int output_size;
    size_t error_location;
    const char *error_message;
};

struct _cffi_externpy_s {
    const char *name;
    size_t size_of_result;
    void *reserved1, *reserved2;
};

#ifdef _CFFI_INTERNAL
static int parse_c_type(struct _cffi_parse_info_s *info, const char *input);
static int search_in_globals(const struct _cffi_type_context_s *ctx,
                             const char *search, size_t search_len);
static int search_in_struct_unions(const struct _cffi_type_context_s *ctx,
                                   const char *search, size_t search_len);
#endif

/* this block of #ifs should be kept exactly identical between
   c/_cffi_backend.c, cffi/vengine_cpy.py, cffi/vengine_gen.py
   and cffi/_cffi_include.h */
#if defined(_MSC_VER)
# include <malloc.h>   /* for alloca() */
# if _MSC_VER < 1600   /* MSVC < 2010 */
   typedef __int8 int8_t;
   typedef __int16 int16_t;
   typedef __int32 int32_t;
   typedef __int64 int64_t;
   typedef unsigned __int8 uint8_t;
   typedef unsigned __int16 uint16_t;
   typedef unsigned __int32 uint32_t;
   typedef unsigned __int64 uint64_t;
   typedef __int8 int_least8_t;
   typedef __int16 int_least16_t;
   typedef __int32 int_least32_t;
   typedef __int64 int_least64_t;
   typedef unsigned __int8 uint_least8_t;
   typedef unsigned __int16 uint_least16_t;
   typedef unsigned __int32 uint_least32_t;
   typedef unsigned __int64 uint_least64_t;
   typedef __int8 int_fast8_t;
   typedef __int16 int_fast16_t;
   typedef __int32 int_fast32_t;
   typedef __int64 int_fast64_t;
   typedef unsigned __int8 uint_fast8_t;
   typedef unsigned __int16 uint_fast16_t;
   typedef unsigned __int32 uint_fast32_t;
   typedef unsigned __int64 uint_fast64_t;
   typedef __int64 intmax_t;
   typedef unsigned __int64 uintmax_t;
# else
#  include <stdint.h>
# endif
# if _MSC_VER < 1800   /* MSVC < 2013 */
#  ifndef __cplusplus
    typedef unsigned char _Bool;
#  endif
# endif
# define _cffi_float_complex_t   _Fcomplex    /* include <complex.h> for it */
# define _cffi_double_complex_t  _Dcomplex    /* include <complex.h> for it */
#else
# include <stdint.h>
# if (defined (__SVR4) && defined (__sun)) || defined(_AIX) || defined(__hpux)
#  include <alloca.h>
# endif
# define _cffi_float_complex_t   float _Complex
# define _cffi_double_complex_t  double _Complex
#endif

#ifdef __GNUC__
# define _CFFI_UNUSED_FN  __attribute__((unused))
#else
# define _CFFI_UNUSED_FN  /* nothing */
#endif

#ifdef __cplusplus
# ifndef _Bool
   typedef bool _Bool;   /* semi-hackish: C++ has no _Bool; bool is builtin */
# endif
#endif

/**********  CPython-specific section  **********/
#ifndef PYPY_VERSION


#if PY_MAJOR_VERSION >= 3
# define PyInt_FromLong PyLong_FromLong
#endif

#define _cffi_from_c_double PyFloat_FromDouble
#define _cffi_from_c_float PyFloat_FromDouble
#define _cffi_from_c_long PyInt_FromLong
#define _cffi_from_c_ulong PyLong_FromUnsignedLong
#define _cffi_from_c_longlong PyLong_FromLongLong
#define _cffi_from_c_ulonglong PyLong_FromUnsignedLongLong
#define _cffi_from_c__Bool PyBool_FromLong

#define _cffi_to_c_double PyFloat_AsDouble
#define _cffi_to_c_float PyFloat_AsDouble

#define _cffi_from_c_int(x, type)                                        \
    (((type)-1) > 0 ? /* unsigned */                                     \
        (sizeof(type) < sizeof(long) ?                                   \
            PyInt_FromLong((long)x) :                                    \
         sizeof(type) == sizeof(long) ?                                  \
            PyLong_FromUnsignedLong((unsigned long)x) :                  \
            PyLong_FromUnsignedLongLong((unsigned long long)x)) :        \
        (sizeof(type) <= sizeof(long) ?                                  \
            PyInt_FromLong((long)x) :                                    \
            PyLong_FromLongLong((long long)x)))

#define _cffi_to_c_int(o, type)                                          \
    ((type)(                                                             \
     sizeof(type) == 1 ? (((type)-1) > 0 ? (type)_cffi_to_c_u8(o)        \
                                         : (type)_cffi_to_c_i8(o)) :     \
     sizeof(type) == 2 ? (((type)-1) > 0 ? (type)_cffi_to_c_u16(o)       \
                                         : (type)_cffi_to_c_i16(o)) :    \
     sizeof(type) == 4 ? (((type)-1) > 0 ? (type)_cffi_to_c_u32(o)       \
                                         : (type)_cffi_to_c_i32(o)) :    \
     sizeof(type) == 8 ? (((type)-1) > 0 ? (type)_cffi_to_c_u64(o)       \
                                         : (type)_cffi_to_c_i64(o)) :    \
     (Py_FatalError("unsupported size for type " #type), (type)0)))

#define _cffi_to_c_i8                                                    \
                 ((int(*)(PyObject *))_cffi_exports[1])
#define _cffi_to_c_u8                                                    \
                 ((int(*)(PyObject *))_cffi_exports[2])
#define _cffi_to_c_i16                                                   \
                 ((int(*)(PyObject *))_cffi_exports[3])
#define _cffi_to_c_u16                                                   \
                 ((int(*)(PyObject *))_cffi_exports[4])
#define _cffi_to_c_i32                                                   \
                 ((int(*)(PyObject *))_cffi_exports[5])
#define _cffi_to_c_u32                                                   \
                 ((unsigned int(*)(PyObject *))_cffi_exports[6])
#define _cffi_to_c_i64                                                   \
                 ((long long(*)(PyObject *))_cffi_exports[7])
#define _cffi_to_c_u64                                                   \
                 ((unsigned long long(*)(PyObject *))_cffi_exports[8])
#define _cffi_to_c_char                                                  \
                 ((int(*)(PyObject *))_cffi_exports[9])
#define _cffi_from_c_pointer                                             \
    ((PyObject *(*)(char *, struct _cffi_ctypedescr *))_cffi_exports[10])
#define _cffi_to_c_pointer                                               \
    ((char *(*)(PyObject *, struct _cffi_ctypedescr *))_cffi_exports[11])
#define _cffi_get_struct_layout                                          \
    not used any more
#define _cffi_restore_errno                                              \
    ((void(*)(void))_cffi_exports[13])
#define _cffi_save_errno                                                 \
    ((void(*)(void))_cffi_exports[14])
#define _cffi_from_c_char                                                \
    ((PyObject *(*)(char))_cffi_exports[15])
#define _cffi_from_c_deref                                               \
    ((PyObject *(*)(char *, struct _cffi_ctypedescr *))_cffi_exports[16])
#define _cffi_to_c                                                       \
    ((int(*)(char *, struct _cffi_ctypedescr *, PyObject *))_cffi_exports[17])
#define _cffi_from_c_struct                                              \
    ((PyObject *(*)(char *, struct _cffi_ctypedescr *))_cffi_exports[18])
#define _cffi_to_c_wchar_t                                               \
    ((_cffi_wchar_t(*)(PyObject *))_cffi_exports[19])
#define _cffi_from_c_wchar_t                                             \
    ((PyObject *(*)(_cffi_wchar_t))_cffi_exports[20])
#define _cffi_to_c_long_double                                           \
    ((long double(*)(PyObject *))_cffi_exports[21])
#define _cffi_to_c__Bool                                                 \
    ((_Bool(*)(PyObject *))_cffi_exports[22])
#define _cffi_prepare_pointer_call_argument                              \
    ((Py_ssize_t(*)(struct _cffi_ctypedescr *,                           \
                    PyObject *, char **))_cffi_exports[23])
#define _cffi_convert_array_from_object                                  \
    ((int(*)(char *, struct _cffi_ctypedescr *, PyObject *))_cffi_exports[24])
#define _CFFI_CPIDX  25
#define _cffi_call_python                                                \
    ((void(*)(struct _cffi_externpy_s *, char *))_cffi_exports[_CFFI_CPIDX])
#define _cffi_to_c_wchar3216_t                                           \
    ((int(*)(PyObject *))_cffi_exports[26])
#define _cffi_from_c_wchar3216_t                                         \
    ((PyObject *(*)(int))_cffi_exports[27])
#define _CFFI_NUM_EXPORTS 28

struct _cffi_ctypedescr;

static void *_cffi_exports[_CFFI_NUM_EXPORTS];

#define _cffi_type(index)   (                           \
    assert((((uintptr_t)_cffi_types[index]) & 1) == 0), \
    (struct _cffi_ctypedescr *)_cffi_types[index])

static PyObject *_cffi_init(const char *module_name, Py_ssize_t version,
                            const struct _cffi_type_context_s *ctx)
{
    PyObject *module, *o_arg, *new_module;
    void *raw[] = {
        (void *)module_name,
        (void *)version,
        (void *)_cffi_exports,
        (void *)ctx,
    };

    module = PyImport_ImportModule("_cffi_backend");
    if (module == NULL)
        goto failure;

    o_arg = PyLong_FromVoidPtr((void *)raw);
    if (o_arg == NULL)
        goto failure;

    new_module = PyObject_CallMethod(
        module, (char *)"_init_cffi_1_0_external_module", (char *)"O", o_arg);

    Py_DECREF(o_arg);
    Py_DECREF(module);
    return new_module;

  failure:
    Py_XDECREF(module);
    return NULL;
}


#ifdef HAVE_WCHAR_H
typedef wchar_t _cffi_wchar_t;
#else
typedef uint16_t _cffi_wchar_t;   /* same random pick as _cffi_backend.c */
#endif

_CFFI_UNUSED_FN static uint16_t _cffi_to_c_char16_t(PyObject *o)
{
    if (sizeof(_cffi_wchar_t) == 2)
        return (uint16_t)_cffi_to_c_wchar_t(o);
    else
        return (uint16_t)_cffi_to_c_wchar3216_t(o);
}

_CFFI_UNUSED_FN static PyObject *_cffi_from_c_char16_t(uint16_t x)
{
    if (sizeof(_cffi_wchar_t) == 2)
        return _cffi_from_c_wchar_t((_cffi_wchar_t)x);
    else
        return _cffi_from_c_wchar3216_t((int)x);
}

_CFFI_UNUSED_FN static int _cffi_to_c_char32_t(PyObject *o)
{
    if (sizeof(_cffi_wchar_t) == 4)
        return (int)_cffi_to_c_wchar_t(o);
    else
        return (int)_cffi_to_c_wchar3216_t(o);
}

_CFFI_UNUSED_FN static PyObject *_cffi_from_c_char32_t(unsigned int x)
{
    if (sizeof(_cffi_wchar_t) == 4)
        return _cffi_from_c_wchar_t((_cffi_wchar_t)x);
    else
        return _cffi_from_c_wchar3216_t((int)x);
}

union _cffi_union_alignment_u {
    unsigned char m_char;
    unsigned short m_short;
    unsigned int m_int;
    unsigned long m_long;
    unsigned long long m_longlong;
    float m_float;
    double m_double;
    long double m_longdouble;
};

struct _cffi_freeme_s {
    struct _cffi_freeme_s *next;
    union _cffi_union_alignment_u alignment;
};

_CFFI_UNUSED_FN static int
_cffi_convert_array_argument(struct _cffi_ctypedescr *ctptr, PyObject *arg,
                             char **output_data, Py_ssize_t datasize,
                             struct _cffi_freeme_s **freeme)
{
    char *p;
    if (datasize < 0)
        return -1;

    p = *output_data;
    if (p == NULL) {
        struct _cffi_freeme_s *fp = (struct _cffi_freeme_s *)PyObject_Malloc(
            offsetof(struct _cffi_freeme_s, alignment) + (size_t)datasize);
        if (fp == NULL)
            return -1;
        fp->next = *freeme;
        *freeme = fp;
        p = *output_data = (char *)&fp->alignment;
    }
    memset((void *)p, 0, (size_t)datasize);
    return _cffi_convert_array_from_object(p, ctptr, arg);
}

_CFFI_UNUSED_FN static void
_cffi_free_array_arguments(struct _cffi_freeme_s *freeme)
{
    do {
        void *p = (void *)freeme;
        freeme = freeme->next;
        PyObject_Free(p);
    } while (freeme != NULL);
}

/**********  end CPython-specific section  **********/
#else
_CFFI_UNUSED_FN
static void (*_cffi_call_python_org)(struct _cffi_externpy_s *, char *);
# define _cffi_call_python  _cffi_call_python_org
#endif


#define _cffi_array_len(array)   (sizeof(array) / sizeof((array)[0]))

#define _cffi_prim_int(size, sign)                                      \
    ((size) == 1 ? ((sign) ? _CFFI_PRIM_INT8  : _CFFI_PRIM_UINT8)  :    \
     (size) == 2 ? ((sign) ? _CFFI_PRIM_INT16 : _CFFI_PRIM_UINT16) :    \
     (size) == 4 ? ((sign) ? _CFFI_PRIM_INT32 : _CFFI_PRIM_UINT32) :    \
     (size) == 8 ? ((sign) ? _CFFI_PRIM_INT64 : _CFFI_PRIM_UINT64) :    \
     _CFFI__UNKNOWN_PRIM)

#define _cffi_prim_float(size)                                          \
    ((size) == sizeof(float) ? _CFFI_PRIM_FLOAT :                       \
     (size) == sizeof(double) ? _CFFI_PRIM_DOUBLE :                     \
     (size) == sizeof(long double) ? _CFFI__UNKNOWN_LONG_DOUBLE :       \
     _CFFI__UNKNOWN_FLOAT_PRIM)

#define _cffi_check_int(got, got_nonpos, expected)      \
    ((got_nonpos) == (expected <= 0) &&                 \
     (got) == (unsigned long long)expected)

#ifdef MS_WIN32
# define _cffi_stdcall  __stdcall
#else
# define _cffi_stdcall  /* nothing */
#endif

#ifdef __cplusplus
}
#endif

/************************************************************/


    struct ptrace_regs_struct
    {
        unsigned long r15;
        unsigned long r14;
        unsigned long r13;
        unsigned long r12;
        unsigned long rbp;
        unsigned long rbx;
        unsigned long r11;
        unsigned long r10;
        unsigned long r9;
        unsigned long r8;
        unsigned long rax;
        unsigned long rcx;
        unsigned long rdx;
        unsigned long rsi;
        unsigned long rdi;
        unsigned long orig_rax;
        unsigned long rip;
        unsigned long cs;
        unsigned long eflags;
        unsigned long rsp;
        unsigned long ss;
        unsigned long fs_base;
        unsigned long gs_base;
        unsigned long ds;
        unsigned long es;
        unsigned long fs;
        unsigned long gs;
    };
    
    #define ARCH_AMD64
    
        struct reg_128
        {
            unsigned char data[16];
        };

        // For details about the layout of the xsave structure, see Intel's Architecture Instruction Set Extensions Programming Reference
        // Chapter 3.2.4 "The Layout of XSAVE Save Area"
        // https://www.intel.com/content/dam/develop/external/us/en/documents/319433-024-697869.pdf
        #pragma pack(push, 1)
        struct fp_regs_struct
        {
            unsigned long type;
            _Bool dirty; // true if the debugging script has modified the state of the registers
            _Bool fresh; // true if the registers have already been fetched for this state
            unsigned char bool_padding[6];
            unsigned char padding0[32];
            struct reg_128 mmx[8];
            struct reg_128 xmm0[16];
            unsigned char padding1[96];
            // end of the 512 byte legacy region
            unsigned char padding2[64];
            // ymm0 starts at offset 576
            struct reg_128 ymm0[16];
            unsigned char padding3[128];
            struct reg_128 bndregs[4];
            struct reg_128 bndcfg;
            unsigned char padding4[48];
            unsigned char padding6[1600];
            unsigned int pkru;
            unsigned char padding7[60];
        };
        #pragma pack(pop)
        
        #define FPREGS_AVX 1
        
        #define XSAVE 1
        
    #define INSTRUCTION_POINTER(regs) (regs.rip)
    #define INSTALL_BREAKPOINT(instruction) ((instruction & 0xFFFFFFFFFFFFFF00) | 0xCC)
    #define BREAKPOINT_SIZE 1
    #define IS_SW_BREAKPOINT(instruction) (instruction == 0xCC)
    
    // X86_64 Architecture specific
    #define IS_RET_INSTRUCTION(instruction) (instruction == 0xC3 || instruction == 0xCB || instruction == 0xC2 || instruction == 0xCA)
    
    int IS_CALL_INSTRUCTION(uint8_t* instr)
    {
        // Check for direct CALL (E8 xx xx xx xx)
        if (instr[0] == (uint8_t)0xE8) {
            return 1; // It's a CALL
        }
        
        // Check for indirect CALL using ModR/M (FF /2)
        if (instr[0] == (uint8_t)0xFF) {
            // Extract ModR/M byte
            uint8_t modRM = (uint8_t)instr[1];
            uint8_t reg = (modRM >> 3) & 7; // Middle three bits

            if (reg == 2) {
                return 1; // It's a CALL
            }
        }

        return 0; // Not a CALL
    }
    //
// This file is part of libdebug Python library (https://github.com/libdebug/libdebug).
// Copyright (c) 2023-2024 Roberto Alessandro Bertolini, Gabriele Digregorio, Francesco Panebianco. All rights reserved.
// Licensed under the MIT license. See LICENSE file in the project root for details.
//

#include <elf.h>
#include <errno.h>
#include <signal.h>
#include <stdint.h>
#include <stdio.h>
#include <string.h>
#include <sys/ptrace.h>
#include <sys/types.h>
#include <sys/uio.h>
#include <sys/user.h>
#include <sys/wait.h>

// Run some static assertions to ensure that the fp types are correct
#if defined ARCH_AMD64 || defined ARCH_I386
    #ifndef FPREGS_AVX
        #error "FPREGS_AVX must be defined"
    #endif

    #ifndef XSAVE
        #error "XSAVE must be defined"
    #endif

    #if (FPREGS_AVX == 0)
        _Static_assert((sizeof(struct fp_regs_struct) - offsetof(struct fp_regs_struct, padding0)) == 512, "user_fpregs_struct size is not 512 bytes");
    #elif (FPREGS_AVX == 1)
        _Static_assert((sizeof(struct fp_regs_struct) - offsetof(struct fp_regs_struct, padding0)) == 2752, "user_fpregs_struct size is not 2752 bytes");
    #elif (FPREGS_AVX == 2)
        _Static_assert((sizeof(struct fp_regs_struct) - offsetof(struct fp_regs_struct, padding0)) == 2752, "user_fpregs_struct size is not 2752 bytes");
    #else
        #error "FPREGS_AVX must be 0, 1 or 2"
    #endif
#endif

struct ptrace_hit_bp {
    int pid;
    unsigned long addr;
    unsigned long bp_instruction;
    unsigned long prev_instruction;
};

struct software_breakpoint {
    unsigned long addr;
    unsigned long instruction;
    unsigned long patched_instruction;
    char enabled;
    struct software_breakpoint *next;
};

struct hardware_breakpoint {
    unsigned long addr;
    int tid;
    char enabled;
    char type[2];
    char len;
    struct hardware_breakpoint *next;
};

struct thread {
    int tid;
    struct ptrace_regs_struct regs;
    struct fp_regs_struct fpregs;
    int signal_to_forward;
    struct thread *next;
};

struct thread_status {
    int tid;
    int status;
    struct thread_status *next;
};

struct global_state {
    struct thread *t_HEAD;
    struct thread *dead_t_HEAD;
    struct software_breakpoint *sw_b_HEAD;
    struct hardware_breakpoint *hw_b_HEAD;
    _Bool handle_syscall_enabled;
};

#if defined ARCH_AMD64 || defined ARCH_I386
int getregs(int tid, struct ptrace_regs_struct *regs)
{
    return ptrace(PTRACE_GETREGS, tid, NULL, regs);
}

int setregs(int tid, struct ptrace_regs_struct *regs)
{
    return ptrace(PTRACE_SETREGS, tid, NULL, regs);
}
#endif

#ifdef ARCH_AARCH64
int getregs(int tid, struct ptrace_regs_struct *regs)
{
    regs->override_syscall_number = 0;

    struct iovec iov;
    iov.iov_base = regs;
    iov.iov_len = sizeof(struct ptrace_regs_struct);
    return ptrace(PTRACE_GETREGSET, tid, NT_PRSTATUS, &iov);
}

int setregs(int tid, struct ptrace_regs_struct *regs)
{
    struct iovec iov;

    if (regs->override_syscall_number) {
        iov.iov_base = &regs->x8;
        iov.iov_len = sizeof(regs->x8);
        ptrace(PTRACE_SETREGSET, tid, NT_ARM_SYSTEM_CALL, &iov);
        regs->override_syscall_number = 0;
    }

    iov.iov_base = regs;
    iov.iov_len = sizeof(struct ptrace_regs_struct);
    return ptrace(PTRACE_SETREGSET, tid, NT_PRSTATUS, &iov);
}
#endif

#if defined ARCH_AMD64 || defined ARCH_I386

#define DR_BASE offsetof(struct user, u_debugreg[0])
#define DR_SIZE sizeof(unsigned long)
#define CTRL_LOCAL(x) (1 << (2 * x))
#define CTRL_COND(x) (16 + (4 * x))
#define CTRL_COND_VAL(x) (x == 'x' ? 0 : (x == 'w' ? 1 : 3))
#define CTRL_LEN(x) (18 + (4 * x))
#define CTRL_LEN_VAL(x) (x == 1 ? 0 : (x == 2 ? 1 : (x == 8 ? 2 : 3)))

void install_hardware_breakpoint(struct hardware_breakpoint *bp)
{
    // find a free debug register
    int i;
    for (i = 0; i < 4; i++) {
        unsigned long address = ptrace(PTRACE_PEEKUSER, bp->tid, DR_BASE + i * DR_SIZE);

        if (!address)
            break;
    }

    if (i == 4) {
        perror("No debug registers available");
        return;
    }

    unsigned long ctrl = CTRL_LOCAL(i) | CTRL_COND_VAL(bp->type[0]) << CTRL_COND(i) | CTRL_LEN_VAL(bp->len) << CTRL_LEN(i);

    // read the state from DR7
    unsigned long state = ptrace(PTRACE_PEEKUSER, bp->tid, DR_BASE + 7 * DR_SIZE);

    // reset the state, for good measure
    state &= ~(3 << CTRL_COND(i));
    state &= ~(3 << CTRL_LEN(i));

    // register the breakpoint
    state |= ctrl;

    // write the address and the state
    ptrace(PTRACE_POKEUSER, bp->tid, DR_BASE + i * DR_SIZE, bp->addr);
    ptrace(PTRACE_POKEUSER, bp->tid, DR_BASE + 7 * DR_SIZE, state);
}


void remove_hardware_breakpoint(struct hardware_breakpoint *bp)
{
    // find the register
    int i;
    for (i = 0; i < 4; i++) {
        unsigned long address = ptrace(PTRACE_PEEKUSER, bp->tid, DR_BASE + i * DR_SIZE);

        if (address == bp->addr)
            break;
    }

    if (i == 4) {
        perror("Breakpoint not found");
        return;
    }

    // read the state from DR7
    unsigned long state = ptrace(PTRACE_PEEKUSER, bp->tid, DR_BASE + 7 * DR_SIZE);

    // reset the state
    state &= ~(3 << CTRL_COND(i));
    state &= ~(3 << CTRL_LEN(i));

    // write the state
    ptrace(PTRACE_POKEUSER, bp->tid, DR_BASE + 7 * DR_SIZE, state);

    // clear the address
    ptrace(PTRACE_POKEUSER, bp->tid, DR_BASE + i * DR_SIZE, 0);
}

int is_breakpoint_hit(struct hardware_breakpoint *bp)
{
    unsigned long status = ptrace(PTRACE_PEEKUSER, bp->tid, DR_BASE + 6 * DR_SIZE);

    int index;
    if (status & 0x1)
        index = 0;
    else if (status & 0x2)
        index = 1;
    else if (status & 0x4)
        index = 2;
    else if (status & 0x8)
        index = 3;
    else
        return 0;

    unsigned long address = ptrace(PTRACE_PEEKUSER, bp->tid, DR_BASE + index * DR_SIZE);

    if (address == bp->addr)
        return 1;

    return 0;
}

int get_remaining_hw_breakpoint_count(struct global_state *state, int tid)
{
    int i;
    for (i = 0; i < 4; i++) {
        unsigned long address = ptrace(PTRACE_PEEKUSER, tid, DR_BASE + i * DR_SIZE);

        if (!address)
            break;
    }

    return 4 - i;
}

int get_remaining_hw_watchpoint_count(struct global_state *state, int tid)
{
    return get_remaining_hw_breakpoint_count(state, tid);
}
#endif

#ifdef ARCH_AARCH64
struct user_hwdebug_state {
    unsigned int dbg_info;
	unsigned int pad;
	struct {
		unsigned long addr;
		unsigned int ctrl;
		unsigned int pad;
	} dbg_regs[16];
};

int get_breakpoint_type(char type[2])
{
    if (type[0] == 'r') {
        if (type[1] == 'w') {
            return 3;
        } else {
            return 1;
        }
    } else if (type[0] == 'w') {
        return 2;
    } else if (type[0] == 'x') {
        return 0;
    } else {
        return -1;
    }
}

void install_hardware_breakpoint(struct hardware_breakpoint *bp)
{
    // find a free debug register
    struct user_hwdebug_state state = {0};

    struct iovec iov;
    iov.iov_base = &state;
    iov.iov_len = sizeof state;

    unsigned long command = get_breakpoint_type(bp->type) == 0 ? NT_ARM_HW_BREAK : NT_ARM_HW_WATCH;

    ptrace(PTRACE_GETREGSET, bp->tid, command, &iov);

    int i;
    for (i = 0; i < 16; i++) {
        if (!state.dbg_regs[i].addr)
            break;
    }

    if (i == 16) {
        perror("No debug registers available");
        return;
    }

    if (bp->type[0] == 'x') {
        // Hardware breakpoint can only be of length 4
        bp->len = 4;
    }

    unsigned int length = (1 << bp->len) - 1;
    unsigned int condition = get_breakpoint_type(bp->type);
    unsigned int control = (length << 5) | (condition << 3) | (2 << 1) | 1;

    state.dbg_regs[i].addr = bp->addr;
    state.dbg_regs[i].ctrl = control;

    ptrace(PTRACE_SETREGSET, bp->tid, command, &iov);
}

void remove_hardware_breakpoint(struct hardware_breakpoint *bp)
{
    struct user_hwdebug_state state = {0};

    struct iovec iov;
    iov.iov_base = &state;
    iov.iov_len = sizeof state;

    unsigned long command = get_breakpoint_type(bp->type) == 0 ? NT_ARM_HW_BREAK : NT_ARM_HW_WATCH;

    ptrace(PTRACE_GETREGSET, bp->tid, command, &iov);

    int i;
    for (i = 0; i < 16; i++) {
        if (state.dbg_regs[i].addr == bp->addr)
            break;
    }

    if (i == 16) {
        perror("Breakpoint not found");
        return;
    }

    state.dbg_regs[i].addr = 0;
    state.dbg_regs[i].ctrl = 0;

    ptrace(PTRACE_SETREGSET, bp->tid, command, &iov);
}

int is_breakpoint_hit(struct hardware_breakpoint *bp)
{
    siginfo_t si;

    if (ptrace(PTRACE_GETSIGINFO, bp->tid, NULL, &si) == -1) {
        return 0;
    }

    // Check that the signal is a SIGTRAP and the code is 0x4
    if (!(si.si_signo == SIGTRAP && si.si_code == 0x4)) {
        return 0;
    }
    
    unsigned long addr = (unsigned long) si.si_addr;

    if (addr == bp->addr) {
        return 1;
    }

    return 0;
}

int _get_remaining_count(struct global_state *state, int tid, int command)
{
    struct user_hwdebug_state dbg_state = {0};

    struct iovec iov;
    iov.iov_base = &dbg_state;
    iov.iov_len = sizeof dbg_state;

    ptrace(PTRACE_GETREGSET, tid, command, &iov);

    return dbg_state.dbg_info & 0xff;
}

int get_remaining_hw_breakpoint_count(struct global_state *state, int tid)
{
    return _get_remaining_count(state, tid, NT_ARM_HW_BREAK);
}

int get_remaining_hw_watchpoint_count(struct global_state *state, int tid)
{
    return _get_remaining_count(state, tid, NT_ARM_HW_WATCH);
}
#endif

struct thread *get_thread(struct global_state *state, int tid)
{
    struct thread *t = state->t_HEAD;
    while (t != NULL) {
        if (t->tid == tid) return t;
        t = t->next;
    }

    return NULL;
}

struct fp_regs_struct *get_thread_fp_regs(struct global_state *state, int tid)
{
    struct thread *t = get_thread(state, tid);

    if (t) {
        return &t->fpregs;
    }

    return NULL;
}

#if defined ARCH_AMD64 || defined ARCH_I386
void get_fp_regs(int tid, struct fp_regs_struct *fpregs)
{
    #if (XSAVE == 0)

    #else
        struct iovec iov;

        iov.iov_base = (unsigned char *)(fpregs) + offsetof(struct fp_regs_struct, padding0);
        iov.iov_len = sizeof(struct fp_regs_struct) - offsetof(struct fp_regs_struct, padding0);

        if (ptrace(PTRACE_GETREGSET, tid, NT_X86_XSTATE, &iov) == -1) {
            perror("ptrace_getregset_xstate");
        }
    #endif

    fpregs->fresh = 1;
}

void set_fp_regs(int tid, struct fp_regs_struct *fpregs)
{
    #if (XSAVE == 0)

    #else
        struct iovec iov;

        iov.iov_base = (unsigned char *)(fpregs) + offsetof(struct fp_regs_struct, padding0);
        iov.iov_len = sizeof(struct fp_regs_struct) - offsetof(struct fp_regs_struct, padding0);

        if (ptrace(PTRACE_SETREGSET, tid, NT_X86_XSTATE, &iov) == -1) {
            perror("ptrace_setregset_xstate");
        }
    #endif

    fpregs->dirty = 0;
    fpregs->fresh = 0;
}
#endif

#ifdef ARCH_AARCH64
void get_fp_regs(int tid, struct fp_regs_struct *fpregs)
{
    struct iovec iov;

    iov.iov_base = (unsigned char *)(fpregs) + offsetof(struct fp_regs_struct, vregs);
    iov.iov_len = sizeof(struct fp_regs_struct) - offsetof(struct fp_regs_struct, vregs);

    if (ptrace(PTRACE_GETREGSET, tid, NT_FPREGSET, &iov) == -1) {
        perror("ptrace_getregset_xstate");
    }

    fpregs->fresh = 1;
}

void set_fp_regs(int tid, struct fp_regs_struct *fpregs)
{
    struct iovec iov;

    iov.iov_base = (unsigned char *)(fpregs) + offsetof(struct fp_regs_struct, vregs);
    iov.iov_len = sizeof(struct fp_regs_struct) - offsetof(struct fp_regs_struct, vregs);

    if (ptrace(PTRACE_SETREGSET, tid, NT_FPREGSET, &iov) == -1) {
        perror("ptrace_setregset_xstate");
    }

    fpregs->dirty = 0;
    fpregs->fresh = 0;
}
#endif

void check_and_set_fp_regs(struct thread *t)
{
    if (t->fpregs.dirty) {
        set_fp_regs(t->tid, &t->fpregs);
    }

    t->fpregs.fresh = 0;
}

struct ptrace_regs_struct *register_thread(struct global_state *state, int tid)
{
    // Verify if the thread is already registered
    struct thread *t = state->t_HEAD;
    while (t != NULL) {
        if (t->tid == tid) return &t->regs;
        t = t->next;
    }

    t = malloc(sizeof(struct thread));
    t->tid = tid;
    t->signal_to_forward = 0;

#if defined ARCH_AMD64 || defined ARCH_I386
    t->fpregs.type = FPREGS_AVX;
#endif
    t->fpregs.dirty = 0;
    t->fpregs.fresh = 0;

    getregs(tid, &t->regs);

    t->next = state->t_HEAD;
    state->t_HEAD = t;

    return &t->regs;
}

void unregister_thread(struct global_state *state, int tid)
{
    struct thread *t = state->t_HEAD;
    struct thread *prev = NULL;

    while (t != NULL) {
        if (t->tid == tid) {
            if (prev == NULL) {
                state->t_HEAD = t->next;
            } else {
                prev->next = t->next;
            }
            // Add the thread to the dead list
            t->next = state->dead_t_HEAD;
            state->dead_t_HEAD = t;
            break;
        }
        prev = t;
        t = t->next;
    }

    struct hardware_breakpoint *hw_b = state->hw_b_HEAD;
    struct hardware_breakpoint *hw_b_prev = NULL;

    while (hw_b != NULL) {
        if (hw_b->tid == tid) {
            if (hw_b_prev == NULL) {
                state->hw_b_HEAD = hw_b->next;
            } else {
                hw_b_prev->next = hw_b->next;
            }
            free(hw_b);
            return;
        }
        hw_b_prev = hw_b;
        hw_b = hw_b->next;
    }
}

void free_thread_list(struct global_state *state)
{
    struct thread *t = state->t_HEAD;
    struct thread *next;

    while (t != NULL) {
        next = t->next;
        free(t);
        t = next;
    }

    state->t_HEAD = NULL;

    t = state->dead_t_HEAD;

    while (t != NULL) {
        next = t->next;
        free(t);
        t = next;
    }

    state->dead_t_HEAD = NULL;
}

int ptrace_trace_me(void)
{
    return ptrace(PTRACE_TRACEME, 0, NULL, NULL);
}

int ptrace_attach(int pid)
{
    return ptrace(PTRACE_ATTACH, pid, NULL, NULL);
}

void ptrace_detach_for_kill(struct global_state *state, int pid)
{
    struct thread *t = state->t_HEAD;
    // note that the order is important: the main thread must be detached last
    while (t != NULL) {
        // let's attempt to read the registers of the thread
        if (getregs(t->tid, &t->regs)) {
            // if we can't read the registers, the thread is probably still running
            // ensure that the thread is stopped
            tgkill(pid, t->tid, SIGSTOP);

            // wait for it to stop
            waitpid(t->tid, NULL, 0);
        }

        // detach from it
        if (ptrace(PTRACE_DETACH, t->tid, NULL, NULL))
            fprintf(stderr, "ptrace_detach failed for thread %d: %s\\n", t->tid,
                    strerror(errno));

        // kill it
        tgkill(pid, t->tid, SIGKILL);

        t = t->next;
    }

    waitpid(pid, NULL, 0);
}

void ptrace_detach_for_migration(struct global_state *state, int pid)
{
    struct thread *t = state->t_HEAD;
    // note that the order is important: the main thread must be detached last
    while (t != NULL) {
        // the user might have modified the state of the registers
        // so we use SETREGS to check if the process is running
        if (setregs(t->tid, &t->regs)) {
            // if we can't read the registers, the thread is probably still running
            // ensure that the thread is stopped
            tgkill(pid, t->tid, SIGSTOP);

            // wait for it to stop
            waitpid(t->tid, NULL, 0);

            // set the registers again, as the first time it failed
            setregs(t->tid, &t->regs);
            check_and_set_fp_regs(t);
        }

        // Be sure that the thread will not run during gdb reattachment
        tgkill(pid, t->tid, SIGSTOP);

        // detach from it
        if (ptrace(PTRACE_DETACH, t->tid, NULL, NULL))
            fprintf(stderr, "ptrace_detach failed for thread %d: %s\\n", t->tid,
                    strerror(errno));

        t = t->next;
    }
}

void ptrace_reattach_from_gdb(struct global_state *state, int pid)
{
    struct thread *t = state->t_HEAD;
    // note that the order is important: the main thread must be detached last
    while (t != NULL) {
        if (ptrace(PTRACE_ATTACH, t->tid, NULL, NULL))
            fprintf(stderr, "ptrace_attach failed for thread %d: %s\\n", t->tid,
                    strerror(errno));

        if (getregs(t->tid, &t->regs))
            fprintf(stderr, "ptrace_getregs failed for thread %d: %s\\n", t->tid,
                    strerror(errno));

        t = t->next;
    }
}

void ptrace_detach_and_cont(struct global_state *state, int pid)
{
    ptrace_detach_for_migration(state, pid);

    // continue the execution of the process
    kill(pid, SIGCONT);
}

void ptrace_set_options(int pid)
{
    int options = PTRACE_O_TRACEFORK | PTRACE_O_TRACEVFORK | PTRACE_O_TRACESYSGOOD |
                  PTRACE_O_TRACECLONE | PTRACE_O_TRACEEXEC | PTRACE_O_TRACEEXIT;

    ptrace(PTRACE_SETOPTIONS, pid, NULL, options);
}

unsigned long ptrace_peekdata(int pid, unsigned long addr)
{
    // Since the value returned by a successful PTRACE_PEEK*
    // request may be -1, the caller must clear errno before the call,
    errno = 0;

    return ptrace(PTRACE_PEEKDATA, pid, (void *)addr, NULL);
}

unsigned long ptrace_pokedata(int pid, unsigned long addr, unsigned long data)
{
    return ptrace(PTRACE_POKEDATA, pid, (void *)addr, data);
}

unsigned long ptrace_geteventmsg(int pid)
{
    unsigned long data = 0;

    ptrace(PTRACE_GETEVENTMSG, pid, NULL, &data);

    return data;
}

long singlestep(struct global_state *state, int tid)
{
    // flush any register changes
    struct thread *t = state->t_HEAD;
    int signal_to_forward = 0;
    while (t != NULL) {
        if (setregs(t->tid, &t->regs))
            perror("ptrace_setregs");

        check_and_set_fp_regs(t);

        if (t->tid == tid) {
            signal_to_forward = t->signal_to_forward;
            t->signal_to_forward = 0;
        }
        t = t->next;
    }

#if defined ARCH_AMD64 || defined ARCH_I386
    return ptrace(PTRACE_SINGLESTEP, tid, NULL, signal_to_forward);
#endif

#ifdef ARCH_AARCH64
    // Cannot singlestep if we are stopped on a hardware breakpoint
    // So we have to check for this, remove it, singlestep and then re-add it
    struct hardware_breakpoint *bp = state->hw_b_HEAD;

    while (bp != NULL) {
        if (bp->tid == tid && bp->enabled && is_breakpoint_hit(bp)) {
            remove_hardware_breakpoint(bp);
            long ret = ptrace(PTRACE_SINGLESTEP, tid, NULL, signal_to_forward);
            install_hardware_breakpoint(bp);
            return ret;
        }
        bp = bp->next;
    }

    return ptrace(PTRACE_SINGLESTEP, tid, NULL, signal_to_forward);
#endif
}

int step_until(struct global_state *state, int tid, unsigned long addr, int max_steps)
{
    // flush any register changes
    struct thread *t = state->t_HEAD, *stepping_thread = NULL;
    while (t != NULL) {
        if (setregs(t->tid, &t->regs))
            perror("ptrace_setregs");

        check_and_set_fp_regs(t);

        if (t->tid == tid)
            stepping_thread = t;

        t = t->next;
    }

    int count = 0, status = 0;
    unsigned long previous_ip;

    if (!stepping_thread) {
        perror("Thread not found");
        return -1;
    }

    // remove any hardware breakpoint that might be set on the stepping thread
    struct hardware_breakpoint *bp = state->hw_b_HEAD;

    while (bp != NULL) {
        if (bp->tid == tid && bp->enabled) {
            remove_hardware_breakpoint(bp);
        }
        bp = bp->next;
    }

    while (max_steps == -1 || count < max_steps) {
        if (ptrace(PTRACE_SINGLESTEP, tid, NULL, NULL)) return -1;

        // wait for the child
        waitpid(tid, &status, 0);

        previous_ip = INSTRUCTION_POINTER(stepping_thread->regs);

        // update the registers
        getregs(tid, &stepping_thread->regs);

        if (INSTRUCTION_POINTER(stepping_thread->regs) == addr) break;

        // if the instruction pointer didn't change, we have to step again
        // because we hit a hardware breakpoint
        if (INSTRUCTION_POINTER(stepping_thread->regs) == previous_ip) continue;

        count++;
    }

    // re-add the hardware breakpoints
    bp = state->hw_b_HEAD;

    while (bp != NULL) {
        if (bp->tid == tid && bp->enabled) {
            install_hardware_breakpoint(bp);
        }
        bp = bp->next;
    }

    return 0;
}

int prepare_for_run(struct global_state *state, int pid)
{
    int status = 0;

    // flush any register changes
    struct thread *t = state->t_HEAD;
    while (t != NULL) {
        if (setregs(t->tid, &t->regs))
            fprintf(stderr, "ptrace_setregs failed for thread %d: %s\\n",
                    t->tid, strerror(errno));

        check_and_set_fp_regs(t);

        t = t->next;
    }

    // iterate over all the threads and check if any of them has hit a software
    // breakpoint
    t = state->t_HEAD;
    struct software_breakpoint *b;
    int t_hit;

    while (t != NULL) {
        t_hit = 0;
        unsigned long ip = INSTRUCTION_POINTER(t->regs);

        b = state->sw_b_HEAD;
        while (b != NULL && !t_hit) {
            if (b->addr == ip)
                // we hit a software breakpoint on this thread
                t_hit = 1;

            b = b->next;
        }

        if (t_hit) {
            // step over the breakpoint
            if (ptrace(PTRACE_SINGLESTEP, t->tid, NULL, NULL)) return -1;

            // wait for the child
            waitpid(t->tid, &status, 0);

            // status == 4991 ==> (WIFSTOPPED(status) && WSTOPSIG(status) ==
            // SIGSTOP) this should happen only if threads are involved
            if (status == 4991) {
                ptrace(PTRACE_SINGLESTEP, t->tid, NULL, NULL);
                waitpid(t->tid, &status, 0);
            }
        }

        t = t->next;
    }

#ifdef ARCH_AARCH64
    // iterate over all the threads and check if any of them has hit a hardware
    // breakpoint
    t = state->t_HEAD;
    struct hardware_breakpoint *bp;
    int bp_hit;

    while (t != NULL) {
        bp_hit = 0;

        bp = state->hw_b_HEAD;
        while (bp != NULL && !bp_hit) {
            if (bp->tid == t->tid && bp->enabled && is_breakpoint_hit(bp)) {
                // we hit a hardware breakpoint on this thread
                bp_hit = 1;
                break;
            }

            bp = bp->next;
        }

        if (bp_hit) {
            // remove the breakpoint
            remove_hardware_breakpoint(bp);

            // step over the breakpoint
            if (ptrace(PTRACE_SINGLESTEP, t->tid, NULL, NULL)) return -1;

            // wait for the child
            waitpid(t->tid, &status, 0);

            // re-add the breakpoint
            install_hardware_breakpoint(bp);
        }

        t = t->next;
    }
#endif

    // Reset any software breakpoint
    b = state->sw_b_HEAD;
    while (b != NULL) {
        if (b->enabled) {
            ptrace(PTRACE_POKEDATA, pid, (void *)b->addr,
                   b->patched_instruction);
        }
        b = b->next;
    }

    return status;
}

int cont_all_and_set_bps(struct global_state *state, int pid)
{
    int status = prepare_for_run(state, pid);

    // continue the execution of all the threads
    struct thread *t = state->t_HEAD;
    while (t != NULL) {
        if (ptrace(state->handle_syscall_enabled
 ? PTRACE_SYSCALL : PTRACE_CONT, t->tid, NULL, t->signal_to_forward))
            fprintf(stderr, "ptrace_cont failed for thread %d with signal %d: %s\\n", t->tid, t->signal_to_forward,
                    strerror(errno));
        t->signal_to_forward = 0;
        t = t->next;
    }

    return status;
}

struct thread_status *wait_all_and_update_regs(struct global_state *state, int pid)
{
    // Allocate the head of the list
    struct thread_status *head;
    head = malloc(sizeof(struct thread_status));
    head->next = NULL;

    // The first element is the first status we get from polling with waitpid
    head->tid = waitpid(-getpgid(pid), &head->status, 0);

    if (head->tid == -1) {
        free(head);
        perror("waitpid");
        return NULL;
    }

    // We must interrupt all the other threads with a SIGSTOP
    struct thread *t = state->t_HEAD;
    int temp_tid, temp_status;
    while (t != NULL) {
        if (t->tid != head->tid) {
            // If GETREGS succeeds, the thread is already stopped, so we must
            // not "stop" it again
            if (getregs(t->tid, &t->regs) == -1) {
                // Stop the thread with a SIGSTOP
                tgkill(pid, t->tid, SIGSTOP);
                // Wait for the thread to stop
                temp_tid = waitpid(t->tid, &temp_status, 0);

                // Register the status of the thread, as it might contain useful
                // information
                struct thread_status *ts = malloc(sizeof(struct thread_status));
                ts->tid = temp_tid;
                ts->status = temp_status;
                ts->next = head;
                head = ts;
            }
        }
        t = t->next;
    }

    // We keep polling but don't block, we want to get all the statuses we can
    while ((temp_tid = waitpid(-getpgid(pid), &temp_status, WNOHANG)) > 0) {
        struct thread_status *ts = malloc(sizeof(struct thread_status));
        ts->tid = temp_tid;
        ts->status = temp_status;
        ts->next = head;
        head = ts;
    }

    // Update the registers of all the threads
    t = state->t_HEAD;
    while (t) {
        getregs(t->tid, &t->regs);
        t = t->next;
    }

    // Restore any software breakpoint
    struct software_breakpoint *b = state->sw_b_HEAD;

    while (b != NULL) {
        if (b->enabled) {
            ptrace(PTRACE_POKEDATA, pid, (void *)b->addr, b->instruction);
        }
        b = b->next;
    }

    return head;
}

void free_thread_status_list(struct thread_status *head)
{
    struct thread_status *next;

    while (head) {
        next = head->next;
        free(head);
        head = next;
    }
}

void register_breakpoint(struct global_state *state, int pid, unsigned long address)
{
    unsigned long instruction, patched_instruction;

    instruction = ptrace(PTRACE_PEEKDATA, pid, (void *)address, NULL);

    patched_instruction = INSTALL_BREAKPOINT(instruction);

    ptrace(PTRACE_POKEDATA, pid, (void *)address, patched_instruction);

    struct software_breakpoint *b = state->sw_b_HEAD;

    while (b != NULL) {
        if (b->addr == address) {
            b->enabled = 1;
            return;
        }
        b = b->next;
    }

    b = malloc(sizeof(struct software_breakpoint));
    b->addr = address;
    b->instruction = instruction;
    b->patched_instruction = patched_instruction;
    b->enabled = 1;

    // Breakpoints should be inserted ordered by address, increasing
    // This is important, because we don't want a breakpoint patching another
    if (state->sw_b_HEAD == NULL || state->sw_b_HEAD->addr > address) {
        b->next = state->sw_b_HEAD;
        state->sw_b_HEAD = b;
        return;
    } else {
        struct software_breakpoint *prev = state->sw_b_HEAD;
        struct software_breakpoint *next = state->sw_b_HEAD->next;

        while (next != NULL && next->addr < address) {
            prev = next;
            next = next->next;
        }

        b->next = next;
        prev->next = b;
    }
}

void unregister_breakpoint(struct global_state *state, unsigned long address)
{
    struct software_breakpoint *b = state->sw_b_HEAD;
    struct software_breakpoint *prev = NULL;

    while (b != NULL) {
        if (b->addr == address) {
            if (prev == NULL) {
                state->sw_b_HEAD = b->next;
            } else {
                prev->next = b->next;
            }
            free(b);
            return;
        }
        prev = b;
        b = b->next;
    }
}

void enable_breakpoint(struct global_state *state, unsigned long address)
{
    struct software_breakpoint *b = state->sw_b_HEAD;

    while (b != NULL) {
        if (b->addr == address) {
            b->enabled = 1;
            break;
        }
        b = b->next;
    }

    // Patch the instruction with the breakpoint
    if (b != NULL) {
        ptrace(PTRACE_POKEDATA, state->t_HEAD->tid, (void *)address, b->patched_instruction);
    }
}

void disable_breakpoint(struct global_state *state, unsigned long address)
{
    struct software_breakpoint *b = state->sw_b_HEAD;

    while (b != NULL) {
        if (b->addr == address) {
            b->enabled = 0;
            break;
        }
        b = b->next;
    }

    // Restore the original instruction
    if (b != NULL) {
        ptrace(PTRACE_POKEDATA, state->t_HEAD->tid, (void *)address, b->instruction);
    }
}

void free_breakpoints(struct global_state *state)
{
    struct software_breakpoint *b = state->sw_b_HEAD;
    struct software_breakpoint *next;

    while (b != NULL) {
        next = b->next;
        free(b);
        b = next;
    }

    state->sw_b_HEAD = NULL;

    struct hardware_breakpoint *h = state->hw_b_HEAD;
    struct hardware_breakpoint *next_h;

    while (h != NULL) {
        next_h = h->next;
        free(h);
        h = next_h;
    }

    state->hw_b_HEAD = NULL;
}

#if defined ARCH_AMD64 || defined ARCH_I386
int check_if_dl_trampoline(struct global_state *state, unsigned long instruction_pointer)
{
    // https://codebrowser.dev/glibc/glibc/sysdeps/i386/dl-trampoline.S.html
    //      0xf7fdaf80 <_dl_runtime_resolve+16>: pop    edx
    //      0xf7fdaf81 <_dl_runtime_resolve+17>: mov    ecx,DWORD PTR [esp]
    //      0xf7fdaf84 <_dl_runtime_resolve+20>: mov    DWORD PTR [esp],eax
    //      0xf7fdaf87 <_dl_runtime_resolve+23>: mov    eax,DWORD PTR [esp+0x4]
    // =>   0xf7fdaf8b <_dl_runtime_resolve+27>: ret    0xc
    //      0xf7fdaf8e:  xchg   ax,ax
    //      0xf7fdaf90 <_dl_runtime_profile>:    push   esp
    //      0xf7fdaf91 <_dl_runtime_profile+1>:  add    DWORD PTR [esp],0x8
    //      0xf7fdaf95 <_dl_runtime_profile+5>:  push   ebp
    //      0xf7fdaf96 <_dl_runtime_profile+6>:  push   eax
    //      0xf7fdaf97 <_dl_runtime_profile+7>:  push   ecx
    //      0xf7fdaf98 <_dl_runtime_profile+8>:  push   edx

    // https://elixir.bootlin.com/glibc/glibc-2.35/source/sysdeps/i386/dl-trampoline.S
    //      0xf7fd9004 <_dl_runtime_resolve+20>:	pop    edx
    //      0xf7fd9005 <_dl_runtime_resolve+21>:	mov    ecx,DWORD PTR [esp]
    //      0xf7fd9008 <_dl_runtime_resolve+24>:	mov    DWORD PTR [esp],eax
    //      0xf7fd900b <_dl_runtime_resolve+27>:	mov    eax,DWORD PTR [esp+0x4]
    // =>   0xf7fd900f <_dl_runtime_resolve+31>:	ret    0xc
    //      0xf7fd9012:	lea    esi,[esi+eiz*1+0x0]
    //      0xf7fd9019:	lea    esi,[esi+eiz*1+0x0]
    //      0xf7fd9020 <_dl_runtime_resolve_shstk>:	endbr32
    //      0xf7fd9024 <_dl_runtime_resolve_shstk+4>:	push   eax
    //      0xf7fd9025 <_dl_runtime_resolve_shstk+5>:	push   edx

    unsigned long data;

    // if ((instruction_pointer & 0xf) != 0xb) {
    //     return 0;
    // }
    // breaks if libc is compiled with CET

    instruction_pointer -= 0xb;

    data = ptrace(PTRACE_PEEKDATA, state->t_HEAD->tid, (void *)instruction_pointer, NULL);
    data = data & 0xFFFFFFFF; // on i386 we get 4 bytes from the ptrace call, while on amd64 we get 8 bytes

    if (data != 0x240c8b5a) {
        return 0;
    }

    instruction_pointer += 0x4;

    data = ptrace(PTRACE_PEEKDATA, state->t_HEAD->tid, (void *)instruction_pointer, NULL);
    data = data & 0xFFFFFFFF;

    if (data != 0x8b240489) {
        return 0;
    }

    instruction_pointer += 0x4;

    data = ptrace(PTRACE_PEEKDATA, state->t_HEAD->tid, (void *)instruction_pointer, NULL);
    data = data & 0xFFFFFFFF;

    if (data != 0xc2042444) {
        return 0;
    }

    instruction_pointer += 0x4;

    data = ptrace(PTRACE_PEEKDATA, state->t_HEAD->tid, (void *)instruction_pointer, NULL);
    data = data & 0xFFFF;

    if (data != 0x000c) {
        return 0;
    }

    return 1;
}
#endif

int stepping_finish(struct global_state *state, int tid, _Bool use_trampoline_heuristic)
{
    int status = prepare_for_run(state, tid);

    struct thread *stepping_thread = state->t_HEAD;
    while (stepping_thread != NULL) {
        if (stepping_thread->tid == tid) {
            break;
        }

        stepping_thread = stepping_thread->next;
    }

    if (!stepping_thread) {
        perror("Thread not found");
        return -1;
    }

    unsigned long previous_ip, current_ip;
    unsigned long opcode_window, opcode;
    struct software_breakpoint *b = state->sw_b_HEAD;

    // We need to keep track of the nested calls
    int nested_call_counter = 1;

    do {
        if (ptrace(PTRACE_SINGLESTEP, tid, NULL, NULL)) return -1;

        // wait for the child
        waitpid(tid, &status, 0);

        previous_ip = INSTRUCTION_POINTER(stepping_thread->regs);

        // update the registers
        getregs(tid, &stepping_thread->regs);

        current_ip = INSTRUCTION_POINTER(stepping_thread->regs);

        // Get value at current instruction pointer
        opcode_window = ptrace(PTRACE_PEEKDATA, tid, (void *)current_ip, NULL);

#if defined ARCH_AMD64 || defined ARCH_I386
        // on amd64 we care only about the first byte
        opcode = opcode_window & 0xFF;
#endif

#ifdef ARCH_AARCH64
        opcode = opcode_window & 0xFFFFFFFF;
#endif

        // if the instruction pointer didn't change, we return
        // because we hit a hardware breakpoint
        // we do the same if we hit a software breakpoint
        if (current_ip == previous_ip || IS_SW_BREAKPOINT(opcode))
            goto cleanup;

        // If we hit a call instruction, we increment the counter
        if (IS_CALL_INSTRUCTION((uint8_t*) &opcode_window))
            nested_call_counter++;
        else if (IS_RET_INSTRUCTION(opcode))
            nested_call_counter--;

#if defined ARCH_AMD64 || defined ARCH_I386
        // On i386, dl-trampoline.S ends with a ret instruction to the resolved address
        // While, on amd64, it ends with a jmp to the resolved address
        // On i386, this decrements the nested_call_counter even if it shouldn't
        // So we need to heuristically check if we are in a dl-trampoline.S
        // And if so, we need to re-increment the nested_call_counter
        // https://codebrowser.dev/glibc/glibc/sysdeps/i386/dl-trampoline.S.html
        if (use_trampoline_heuristic && check_if_dl_trampoline(state, current_ip)) {
            nested_call_counter++;
        }
#endif

    } while (nested_call_counter > 0);

    // We are in a return instruction, do the last step
    if (ptrace(PTRACE_SINGLESTEP, tid, NULL, NULL)) return -1;

    // wait for the child
    waitpid(tid, &status, 0);

    // update the registers
    getregs(tid, &stepping_thread->regs);

cleanup:
    // remove any installed breakpoint
    while (b != NULL) {
        if (b->enabled) {
            ptrace(PTRACE_POKEDATA, tid, (void *)b->addr, b->instruction);
        }
        b = b->next;
    }

    return 0;
}

void register_hw_breakpoint(struct global_state *state, int tid, unsigned long address, char type[2], char len)
{
    struct hardware_breakpoint *b = state->hw_b_HEAD;

    while (b != NULL) {
        if (b->addr == address && b->tid == tid) {
            perror("Breakpoint already registered");
            return;
        }
        b = b->next;
    }

    b = malloc(sizeof(struct hardware_breakpoint));
    b->addr = address;
    b->tid = tid;
    b->enabled = 1;
    b->type[0] = type[0];
    b->type[1] = type[1];
    b->len = len;

    b->next = state->hw_b_HEAD;
    state->hw_b_HEAD = b;

    install_hardware_breakpoint(b);
}

void unregister_hw_breakpoint(struct global_state *state, int tid, unsigned long address)
{
    struct hardware_breakpoint *b = state->hw_b_HEAD;
    struct hardware_breakpoint *prev = NULL;

    while (b != NULL) {
        if (b->addr == address && b->tid == tid) {
            if (prev == NULL) {
                state->hw_b_HEAD = b->next;
            } else {
                prev->next = b->next;
            }

            if (b->enabled) {
                remove_hardware_breakpoint(b);
            }

            free(b);
            return;
        }
        prev = b;
        b = b->next;
    }
}

void enable_hw_breakpoint(struct global_state *state, int tid, unsigned long address)
{
    struct hardware_breakpoint *b = state->hw_b_HEAD;

    while (b != NULL) {
        if (b->addr == address && b->tid == tid) {
            if (!b->enabled) {
                install_hardware_breakpoint(b);
            }

            b->enabled = 1;
        }
        b = b->next;
    }
}

void disable_hw_breakpoint(struct global_state *state, int tid, unsigned long address)
{
    struct hardware_breakpoint *b = state->hw_b_HEAD;

    while (b != NULL) {
        if (b->addr == address && b->tid == tid) {
            if (b->enabled) {
                remove_hardware_breakpoint(b);
            }

            b->enabled = 0;
        }
        b = b->next;
    }
}

unsigned long get_hit_hw_breakpoint(struct global_state *state, int tid)
{
    struct hardware_breakpoint *b = state->hw_b_HEAD;

    while (b != NULL) {
        if (b->tid == tid && is_breakpoint_hit(b)) {
            return b->addr;
        }
        b = b->next;
    }

    return 0;
}


/************************************************************/

static void *_cffi_types[] = {
/*  0 */ _CFFI_OP(_CFFI_OP_FUNCTION, 1), // int()(int)
/*  1 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 7), // int
/*  2 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/*  3 */ _CFFI_OP(_CFFI_OP_FUNCTION, 1), // int()(struct global_state *, int)
/*  4 */ _CFFI_OP(_CFFI_OP_POINTER, 89), // struct global_state *
/*  5 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 7),
/*  6 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/*  7 */ _CFFI_OP(_CFFI_OP_FUNCTION, 1), // int()(struct global_state *, int, _Bool)
/*  8 */ _CFFI_OP(_CFFI_OP_NOOP, 4),
/*  9 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 7),
/* 10 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 1), // _Bool
/* 11 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/* 12 */ _CFFI_OP(_CFFI_OP_FUNCTION, 1), // int()(struct global_state *, int, unsigned long, int)
/* 13 */ _CFFI_OP(_CFFI_OP_NOOP, 4),
/* 14 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 7),
/* 15 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 10), // unsigned long
/* 16 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 7),
/* 17 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/* 18 */ _CFFI_OP(_CFFI_OP_FUNCTION, 1), // int()(void)
/* 19 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/* 20 */ _CFFI_OP(_CFFI_OP_FUNCTION, 87), // long()(struct global_state *, int)
/* 21 */ _CFFI_OP(_CFFI_OP_NOOP, 4),
/* 22 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 7),
/* 23 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/* 24 */ _CFFI_OP(_CFFI_OP_FUNCTION, 57), // struct fp_regs_struct *()(struct global_state *, int)
/* 25 */ _CFFI_OP(_CFFI_OP_NOOP, 4),
/* 26 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 7),
/* 27 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/* 28 */ _CFFI_OP(_CFFI_OP_FUNCTION, 93), // struct ptrace_regs_struct *()(struct global_state *, int)
/* 29 */ _CFFI_OP(_CFFI_OP_NOOP, 4),
/* 30 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 7),
/* 31 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/* 32 */ _CFFI_OP(_CFFI_OP_FUNCTION, 83), // struct thread_status *()(struct global_state *, int)
/* 33 */ _CFFI_OP(_CFFI_OP_NOOP, 4),
/* 34 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 7),
/* 35 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/* 36 */ _CFFI_OP(_CFFI_OP_FUNCTION, 15), // unsigned long()(int)
/* 37 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 7),
/* 38 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/* 39 */ _CFFI_OP(_CFFI_OP_FUNCTION, 15), // unsigned long()(int, unsigned long)
/* 40 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 7),
/* 41 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 10),
/* 42 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/* 43 */ _CFFI_OP(_CFFI_OP_FUNCTION, 15), // unsigned long()(int, unsigned long, unsigned long)
/* 44 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 7),
/* 45 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 10),
/* 46 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 10),
/* 47 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/* 48 */ _CFFI_OP(_CFFI_OP_FUNCTION, 15), // unsigned long()(struct global_state *, int)
/* 49 */ _CFFI_OP(_CFFI_OP_NOOP, 4),
/* 50 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 7),
/* 51 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/* 52 */ _CFFI_OP(_CFFI_OP_FUNCTION, 127), // void()(int)
/* 53 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 7),
/* 54 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/* 55 */ _CFFI_OP(_CFFI_OP_FUNCTION, 127), // void()(int, struct fp_regs_struct *)
/* 56 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 7),
/* 57 */ _CFFI_OP(_CFFI_OP_POINTER, 88), // struct fp_regs_struct *
/* 58 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/* 59 */ _CFFI_OP(_CFFI_OP_FUNCTION, 127), // void()(struct global_state *)
/* 60 */ _CFFI_OP(_CFFI_OP_NOOP, 4),
/* 61 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/* 62 */ _CFFI_OP(_CFFI_OP_FUNCTION, 127), // void()(struct global_state *, int)
/* 63 */ _CFFI_OP(_CFFI_OP_NOOP, 4),
/* 64 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 7),
/* 65 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/* 66 */ _CFFI_OP(_CFFI_OP_FUNCTION, 127), // void()(struct global_state *, int, unsigned long)
/* 67 */ _CFFI_OP(_CFFI_OP_NOOP, 4),
/* 68 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 7),
/* 69 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 10),
/* 70 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/* 71 */ _CFFI_OP(_CFFI_OP_FUNCTION, 127), // void()(struct global_state *, int, unsigned long, char *, char)
/* 72 */ _CFFI_OP(_CFFI_OP_NOOP, 4),
/* 73 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 7),
/* 74 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 10),
/* 75 */ _CFFI_OP(_CFFI_OP_POINTER, 76), // char *
/* 76 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 2), // char
/* 77 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/* 78 */ _CFFI_OP(_CFFI_OP_FUNCTION, 127), // void()(struct global_state *, unsigned long)
/* 79 */ _CFFI_OP(_CFFI_OP_NOOP, 4),
/* 80 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 10),
/* 81 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/* 82 */ _CFFI_OP(_CFFI_OP_FUNCTION, 127), // void()(struct thread_status *)
/* 83 */ _CFFI_OP(_CFFI_OP_POINTER, 106), // struct thread_status *
/* 84 */ _CFFI_OP(_CFFI_OP_FUNCTION_END, 0),
/* 85 */ _CFFI_OP(_CFFI_OP_ARRAY, 76), // char[2]
/* 86 */ (_cffi_opcode_t)(2),
/* 87 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 9), // long
/* 88 */ _CFFI_OP(_CFFI_OP_STRUCT_UNION, 0), // struct fp_regs_struct
/* 89 */ _CFFI_OP(_CFFI_OP_STRUCT_UNION, 1), // struct global_state
/* 90 */ _CFFI_OP(_CFFI_OP_POINTER, 91), // struct hardware_breakpoint *
/* 91 */ _CFFI_OP(_CFFI_OP_STRUCT_UNION, 2), // struct hardware_breakpoint
/* 92 */ _CFFI_OP(_CFFI_OP_STRUCT_UNION, 3), // struct ptrace_hit_bp
/* 93 */ _CFFI_OP(_CFFI_OP_POINTER, 94), // struct ptrace_regs_struct *
/* 94 */ _CFFI_OP(_CFFI_OP_STRUCT_UNION, 4), // struct ptrace_regs_struct
/* 95 */ _CFFI_OP(_CFFI_OP_STRUCT_UNION, 5), // struct reg_128
/* 96 */ _CFFI_OP(_CFFI_OP_ARRAY, 95), // struct reg_128[16]
/* 97 */ (_cffi_opcode_t)(16),
/* 98 */ _CFFI_OP(_CFFI_OP_ARRAY, 95), // struct reg_128[4]
/* 99 */ (_cffi_opcode_t)(4),
/* 100 */ _CFFI_OP(_CFFI_OP_ARRAY, 95), // struct reg_128[8]
/* 101 */ (_cffi_opcode_t)(8),
/* 102 */ _CFFI_OP(_CFFI_OP_POINTER, 103), // struct software_breakpoint *
/* 103 */ _CFFI_OP(_CFFI_OP_STRUCT_UNION, 6), // struct software_breakpoint
/* 104 */ _CFFI_OP(_CFFI_OP_POINTER, 105), // struct thread *
/* 105 */ _CFFI_OP(_CFFI_OP_STRUCT_UNION, 7), // struct thread
/* 106 */ _CFFI_OP(_CFFI_OP_STRUCT_UNION, 8), // struct thread_status
/* 107 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 4), // unsigned char
/* 108 */ _CFFI_OP(_CFFI_OP_ARRAY, 107), // unsigned char[128]
/* 109 */ (_cffi_opcode_t)(128),
/* 110 */ _CFFI_OP(_CFFI_OP_ARRAY, 107), // unsigned char[1600]
/* 111 */ (_cffi_opcode_t)(1600),
/* 112 */ _CFFI_OP(_CFFI_OP_ARRAY, 107), // unsigned char[16]
/* 113 */ (_cffi_opcode_t)(16),
/* 114 */ _CFFI_OP(_CFFI_OP_ARRAY, 107), // unsigned char[32]
/* 115 */ (_cffi_opcode_t)(32),
/* 116 */ _CFFI_OP(_CFFI_OP_ARRAY, 107), // unsigned char[48]
/* 117 */ (_cffi_opcode_t)(48),
/* 118 */ _CFFI_OP(_CFFI_OP_ARRAY, 107), // unsigned char[60]
/* 119 */ (_cffi_opcode_t)(60),
/* 120 */ _CFFI_OP(_CFFI_OP_ARRAY, 107), // unsigned char[64]
/* 121 */ (_cffi_opcode_t)(64),
/* 122 */ _CFFI_OP(_CFFI_OP_ARRAY, 107), // unsigned char[6]
/* 123 */ (_cffi_opcode_t)(6),
/* 124 */ _CFFI_OP(_CFFI_OP_ARRAY, 107), // unsigned char[96]
/* 125 */ (_cffi_opcode_t)(96),
/* 126 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 8), // unsigned int
/* 127 */ _CFFI_OP(_CFFI_OP_PRIMITIVE, 0), // void
};

static int _cffi_d_cont_all_and_set_bps(struct global_state * x0, int x1)
{
  return cont_all_and_set_bps(x0, x1);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_cont_all_and_set_bps(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  int result;
  PyObject *pyresult;
  PyObject *arg0;
  PyObject *arg1;

  if (!PyArg_UnpackTuple(args, "cont_all_and_set_bps", 2, 2, &arg0, &arg1))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { result = cont_all_and_set_bps(x0, x1); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  pyresult = _cffi_from_c_int(result, int);
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  return pyresult;
}
#else
#  define _cffi_f_cont_all_and_set_bps _cffi_d_cont_all_and_set_bps
#endif

static void _cffi_d_disable_breakpoint(struct global_state * x0, unsigned long x1)
{
  disable_breakpoint(x0, x1);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_disable_breakpoint(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  unsigned long x1;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  PyObject *arg0;
  PyObject *arg1;

  if (!PyArg_UnpackTuple(args, "disable_breakpoint", 2, 2, &arg0, &arg1))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, unsigned long);
  if (x1 == (unsigned long)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { disable_breakpoint(x0, x1); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  Py_INCREF(Py_None);
  return Py_None;
}
#else
#  define _cffi_f_disable_breakpoint _cffi_d_disable_breakpoint
#endif

static void _cffi_d_disable_hw_breakpoint(struct global_state * x0, int x1, unsigned long x2)
{
  disable_hw_breakpoint(x0, x1, x2);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_disable_hw_breakpoint(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  unsigned long x2;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  PyObject *arg0;
  PyObject *arg1;
  PyObject *arg2;

  if (!PyArg_UnpackTuple(args, "disable_hw_breakpoint", 3, 3, &arg0, &arg1, &arg2))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  x2 = _cffi_to_c_int(arg2, unsigned long);
  if (x2 == (unsigned long)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { disable_hw_breakpoint(x0, x1, x2); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  Py_INCREF(Py_None);
  return Py_None;
}
#else
#  define _cffi_f_disable_hw_breakpoint _cffi_d_disable_hw_breakpoint
#endif

static void _cffi_d_enable_breakpoint(struct global_state * x0, unsigned long x1)
{
  enable_breakpoint(x0, x1);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_enable_breakpoint(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  unsigned long x1;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  PyObject *arg0;
  PyObject *arg1;

  if (!PyArg_UnpackTuple(args, "enable_breakpoint", 2, 2, &arg0, &arg1))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, unsigned long);
  if (x1 == (unsigned long)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { enable_breakpoint(x0, x1); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  Py_INCREF(Py_None);
  return Py_None;
}
#else
#  define _cffi_f_enable_breakpoint _cffi_d_enable_breakpoint
#endif

static void _cffi_d_enable_hw_breakpoint(struct global_state * x0, int x1, unsigned long x2)
{
  enable_hw_breakpoint(x0, x1, x2);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_enable_hw_breakpoint(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  unsigned long x2;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  PyObject *arg0;
  PyObject *arg1;
  PyObject *arg2;

  if (!PyArg_UnpackTuple(args, "enable_hw_breakpoint", 3, 3, &arg0, &arg1, &arg2))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  x2 = _cffi_to_c_int(arg2, unsigned long);
  if (x2 == (unsigned long)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { enable_hw_breakpoint(x0, x1, x2); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  Py_INCREF(Py_None);
  return Py_None;
}
#else
#  define _cffi_f_enable_hw_breakpoint _cffi_d_enable_hw_breakpoint
#endif

static void _cffi_d_free_breakpoints(struct global_state * x0)
{
  free_breakpoints(x0);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_free_breakpoints(PyObject *self, PyObject *arg0)
{
  struct global_state * x0;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { free_breakpoints(x0); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  Py_INCREF(Py_None);
  return Py_None;
}
#else
#  define _cffi_f_free_breakpoints _cffi_d_free_breakpoints
#endif

static void _cffi_d_free_thread_list(struct global_state * x0)
{
  free_thread_list(x0);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_free_thread_list(PyObject *self, PyObject *arg0)
{
  struct global_state * x0;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { free_thread_list(x0); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  Py_INCREF(Py_None);
  return Py_None;
}
#else
#  define _cffi_f_free_thread_list _cffi_d_free_thread_list
#endif

static void _cffi_d_free_thread_status_list(struct thread_status * x0)
{
  free_thread_status_list(x0);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_free_thread_status_list(PyObject *self, PyObject *arg0)
{
  struct thread_status * x0;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(83), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct thread_status *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(83), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { free_thread_status_list(x0); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  Py_INCREF(Py_None);
  return Py_None;
}
#else
#  define _cffi_f_free_thread_status_list _cffi_d_free_thread_status_list
#endif

static void _cffi_d_get_fp_regs(int x0, struct fp_regs_struct * x1)
{
  get_fp_regs(x0, x1);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_get_fp_regs(PyObject *self, PyObject *args)
{
  int x0;
  struct fp_regs_struct * x1;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  PyObject *arg0;
  PyObject *arg1;

  if (!PyArg_UnpackTuple(args, "get_fp_regs", 2, 2, &arg0, &arg1))
    return NULL;

  x0 = _cffi_to_c_int(arg0, int);
  if (x0 == (int)-1 && PyErr_Occurred())
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(57), arg1, (char **)&x1);
  if (datasize != 0) {
    x1 = ((size_t)datasize) <= 640 ? (struct fp_regs_struct *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(57), arg1, (char **)&x1,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { get_fp_regs(x0, x1); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  Py_INCREF(Py_None);
  return Py_None;
}
#else
#  define _cffi_f_get_fp_regs _cffi_d_get_fp_regs
#endif

static unsigned long _cffi_d_get_hit_hw_breakpoint(struct global_state * x0, int x1)
{
  return get_hit_hw_breakpoint(x0, x1);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_get_hit_hw_breakpoint(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  unsigned long result;
  PyObject *pyresult;
  PyObject *arg0;
  PyObject *arg1;

  if (!PyArg_UnpackTuple(args, "get_hit_hw_breakpoint", 2, 2, &arg0, &arg1))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { result = get_hit_hw_breakpoint(x0, x1); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  pyresult = _cffi_from_c_int(result, unsigned long);
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  return pyresult;
}
#else
#  define _cffi_f_get_hit_hw_breakpoint _cffi_d_get_hit_hw_breakpoint
#endif

static int _cffi_d_get_remaining_hw_breakpoint_count(struct global_state * x0, int x1)
{
  return get_remaining_hw_breakpoint_count(x0, x1);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_get_remaining_hw_breakpoint_count(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  int result;
  PyObject *pyresult;
  PyObject *arg0;
  PyObject *arg1;

  if (!PyArg_UnpackTuple(args, "get_remaining_hw_breakpoint_count", 2, 2, &arg0, &arg1))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { result = get_remaining_hw_breakpoint_count(x0, x1); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  pyresult = _cffi_from_c_int(result, int);
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  return pyresult;
}
#else
#  define _cffi_f_get_remaining_hw_breakpoint_count _cffi_d_get_remaining_hw_breakpoint_count
#endif

static int _cffi_d_get_remaining_hw_watchpoint_count(struct global_state * x0, int x1)
{
  return get_remaining_hw_watchpoint_count(x0, x1);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_get_remaining_hw_watchpoint_count(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  int result;
  PyObject *pyresult;
  PyObject *arg0;
  PyObject *arg1;

  if (!PyArg_UnpackTuple(args, "get_remaining_hw_watchpoint_count", 2, 2, &arg0, &arg1))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { result = get_remaining_hw_watchpoint_count(x0, x1); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  pyresult = _cffi_from_c_int(result, int);
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  return pyresult;
}
#else
#  define _cffi_f_get_remaining_hw_watchpoint_count _cffi_d_get_remaining_hw_watchpoint_count
#endif

static struct fp_regs_struct * _cffi_d_get_thread_fp_regs(struct global_state * x0, int x1)
{
  return get_thread_fp_regs(x0, x1);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_get_thread_fp_regs(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  struct fp_regs_struct * result;
  PyObject *pyresult;
  PyObject *arg0;
  PyObject *arg1;

  if (!PyArg_UnpackTuple(args, "get_thread_fp_regs", 2, 2, &arg0, &arg1))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { result = get_thread_fp_regs(x0, x1); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  pyresult = _cffi_from_c_pointer((char *)result, _cffi_type(57));
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  return pyresult;
}
#else
#  define _cffi_f_get_thread_fp_regs _cffi_d_get_thread_fp_regs
#endif

static int _cffi_d_ptrace_attach(int x0)
{
  return ptrace_attach(x0);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_ptrace_attach(PyObject *self, PyObject *arg0)
{
  int x0;
  int result;
  PyObject *pyresult;

  x0 = _cffi_to_c_int(arg0, int);
  if (x0 == (int)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { result = ptrace_attach(x0); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  pyresult = _cffi_from_c_int(result, int);
  return pyresult;
}
#else
#  define _cffi_f_ptrace_attach _cffi_d_ptrace_attach
#endif

static void _cffi_d_ptrace_detach_and_cont(struct global_state * x0, int x1)
{
  ptrace_detach_and_cont(x0, x1);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_ptrace_detach_and_cont(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  PyObject *arg0;
  PyObject *arg1;

  if (!PyArg_UnpackTuple(args, "ptrace_detach_and_cont", 2, 2, &arg0, &arg1))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { ptrace_detach_and_cont(x0, x1); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  Py_INCREF(Py_None);
  return Py_None;
}
#else
#  define _cffi_f_ptrace_detach_and_cont _cffi_d_ptrace_detach_and_cont
#endif

static void _cffi_d_ptrace_detach_for_kill(struct global_state * x0, int x1)
{
  ptrace_detach_for_kill(x0, x1);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_ptrace_detach_for_kill(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  PyObject *arg0;
  PyObject *arg1;

  if (!PyArg_UnpackTuple(args, "ptrace_detach_for_kill", 2, 2, &arg0, &arg1))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { ptrace_detach_for_kill(x0, x1); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  Py_INCREF(Py_None);
  return Py_None;
}
#else
#  define _cffi_f_ptrace_detach_for_kill _cffi_d_ptrace_detach_for_kill
#endif

static void _cffi_d_ptrace_detach_for_migration(struct global_state * x0, int x1)
{
  ptrace_detach_for_migration(x0, x1);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_ptrace_detach_for_migration(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  PyObject *arg0;
  PyObject *arg1;

  if (!PyArg_UnpackTuple(args, "ptrace_detach_for_migration", 2, 2, &arg0, &arg1))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { ptrace_detach_for_migration(x0, x1); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  Py_INCREF(Py_None);
  return Py_None;
}
#else
#  define _cffi_f_ptrace_detach_for_migration _cffi_d_ptrace_detach_for_migration
#endif

static unsigned long _cffi_d_ptrace_geteventmsg(int x0)
{
  return ptrace_geteventmsg(x0);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_ptrace_geteventmsg(PyObject *self, PyObject *arg0)
{
  int x0;
  unsigned long result;
  PyObject *pyresult;

  x0 = _cffi_to_c_int(arg0, int);
  if (x0 == (int)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { result = ptrace_geteventmsg(x0); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  pyresult = _cffi_from_c_int(result, unsigned long);
  return pyresult;
}
#else
#  define _cffi_f_ptrace_geteventmsg _cffi_d_ptrace_geteventmsg
#endif

static unsigned long _cffi_d_ptrace_peekdata(int x0, unsigned long x1)
{
  return ptrace_peekdata(x0, x1);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_ptrace_peekdata(PyObject *self, PyObject *args)
{
  int x0;
  unsigned long x1;
  unsigned long result;
  PyObject *pyresult;
  PyObject *arg0;
  PyObject *arg1;

  if (!PyArg_UnpackTuple(args, "ptrace_peekdata", 2, 2, &arg0, &arg1))
    return NULL;

  x0 = _cffi_to_c_int(arg0, int);
  if (x0 == (int)-1 && PyErr_Occurred())
    return NULL;

  x1 = _cffi_to_c_int(arg1, unsigned long);
  if (x1 == (unsigned long)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { result = ptrace_peekdata(x0, x1); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  pyresult = _cffi_from_c_int(result, unsigned long);
  return pyresult;
}
#else
#  define _cffi_f_ptrace_peekdata _cffi_d_ptrace_peekdata
#endif

static unsigned long _cffi_d_ptrace_pokedata(int x0, unsigned long x1, unsigned long x2)
{
  return ptrace_pokedata(x0, x1, x2);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_ptrace_pokedata(PyObject *self, PyObject *args)
{
  int x0;
  unsigned long x1;
  unsigned long x2;
  unsigned long result;
  PyObject *pyresult;
  PyObject *arg0;
  PyObject *arg1;
  PyObject *arg2;

  if (!PyArg_UnpackTuple(args, "ptrace_pokedata", 3, 3, &arg0, &arg1, &arg2))
    return NULL;

  x0 = _cffi_to_c_int(arg0, int);
  if (x0 == (int)-1 && PyErr_Occurred())
    return NULL;

  x1 = _cffi_to_c_int(arg1, unsigned long);
  if (x1 == (unsigned long)-1 && PyErr_Occurred())
    return NULL;

  x2 = _cffi_to_c_int(arg2, unsigned long);
  if (x2 == (unsigned long)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { result = ptrace_pokedata(x0, x1, x2); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  pyresult = _cffi_from_c_int(result, unsigned long);
  return pyresult;
}
#else
#  define _cffi_f_ptrace_pokedata _cffi_d_ptrace_pokedata
#endif

static void _cffi_d_ptrace_reattach_from_gdb(struct global_state * x0, int x1)
{
  ptrace_reattach_from_gdb(x0, x1);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_ptrace_reattach_from_gdb(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  PyObject *arg0;
  PyObject *arg1;

  if (!PyArg_UnpackTuple(args, "ptrace_reattach_from_gdb", 2, 2, &arg0, &arg1))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { ptrace_reattach_from_gdb(x0, x1); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  Py_INCREF(Py_None);
  return Py_None;
}
#else
#  define _cffi_f_ptrace_reattach_from_gdb _cffi_d_ptrace_reattach_from_gdb
#endif

static void _cffi_d_ptrace_set_options(int x0)
{
  ptrace_set_options(x0);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_ptrace_set_options(PyObject *self, PyObject *arg0)
{
  int x0;

  x0 = _cffi_to_c_int(arg0, int);
  if (x0 == (int)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { ptrace_set_options(x0); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  Py_INCREF(Py_None);
  return Py_None;
}
#else
#  define _cffi_f_ptrace_set_options _cffi_d_ptrace_set_options
#endif

static int _cffi_d_ptrace_trace_me(void)
{
  return ptrace_trace_me();
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_ptrace_trace_me(PyObject *self, PyObject *noarg)
{
  int result;
  PyObject *pyresult;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { result = ptrace_trace_me(); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  (void)noarg; /* unused */
  pyresult = _cffi_from_c_int(result, int);
  return pyresult;
}
#else
#  define _cffi_f_ptrace_trace_me _cffi_d_ptrace_trace_me
#endif

static void _cffi_d_register_breakpoint(struct global_state * x0, int x1, unsigned long x2)
{
  register_breakpoint(x0, x1, x2);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_register_breakpoint(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  unsigned long x2;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  PyObject *arg0;
  PyObject *arg1;
  PyObject *arg2;

  if (!PyArg_UnpackTuple(args, "register_breakpoint", 3, 3, &arg0, &arg1, &arg2))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  x2 = _cffi_to_c_int(arg2, unsigned long);
  if (x2 == (unsigned long)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { register_breakpoint(x0, x1, x2); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  Py_INCREF(Py_None);
  return Py_None;
}
#else
#  define _cffi_f_register_breakpoint _cffi_d_register_breakpoint
#endif

static void _cffi_d_register_hw_breakpoint(struct global_state * x0, int x1, unsigned long x2, char * x3, char x4)
{
  register_hw_breakpoint(x0, x1, x2, x3, x4);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_register_hw_breakpoint(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  unsigned long x2;
  char * x3;
  char x4;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  PyObject *arg0;
  PyObject *arg1;
  PyObject *arg2;
  PyObject *arg3;
  PyObject *arg4;

  if (!PyArg_UnpackTuple(args, "register_hw_breakpoint", 5, 5, &arg0, &arg1, &arg2, &arg3, &arg4))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  x2 = _cffi_to_c_int(arg2, unsigned long);
  if (x2 == (unsigned long)-1 && PyErr_Occurred())
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(75), arg3, (char **)&x3);
  if (datasize != 0) {
    x3 = ((size_t)datasize) <= 640 ? (char *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(75), arg3, (char **)&x3,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x4 = (char)_cffi_to_c_char(arg4);
  if (x4 == (char)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { register_hw_breakpoint(x0, x1, x2, x3, x4); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  Py_INCREF(Py_None);
  return Py_None;
}
#else
#  define _cffi_f_register_hw_breakpoint _cffi_d_register_hw_breakpoint
#endif

static struct ptrace_regs_struct * _cffi_d_register_thread(struct global_state * x0, int x1)
{
  return register_thread(x0, x1);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_register_thread(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  struct ptrace_regs_struct * result;
  PyObject *pyresult;
  PyObject *arg0;
  PyObject *arg1;

  if (!PyArg_UnpackTuple(args, "register_thread", 2, 2, &arg0, &arg1))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { result = register_thread(x0, x1); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  pyresult = _cffi_from_c_pointer((char *)result, _cffi_type(93));
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  return pyresult;
}
#else
#  define _cffi_f_register_thread _cffi_d_register_thread
#endif

static void _cffi_d_set_fp_regs(int x0, struct fp_regs_struct * x1)
{
  set_fp_regs(x0, x1);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_set_fp_regs(PyObject *self, PyObject *args)
{
  int x0;
  struct fp_regs_struct * x1;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  PyObject *arg0;
  PyObject *arg1;

  if (!PyArg_UnpackTuple(args, "set_fp_regs", 2, 2, &arg0, &arg1))
    return NULL;

  x0 = _cffi_to_c_int(arg0, int);
  if (x0 == (int)-1 && PyErr_Occurred())
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(57), arg1, (char **)&x1);
  if (datasize != 0) {
    x1 = ((size_t)datasize) <= 640 ? (struct fp_regs_struct *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(57), arg1, (char **)&x1,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { set_fp_regs(x0, x1); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  Py_INCREF(Py_None);
  return Py_None;
}
#else
#  define _cffi_f_set_fp_regs _cffi_d_set_fp_regs
#endif

static long _cffi_d_singlestep(struct global_state * x0, int x1)
{
  return singlestep(x0, x1);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_singlestep(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  long result;
  PyObject *pyresult;
  PyObject *arg0;
  PyObject *arg1;

  if (!PyArg_UnpackTuple(args, "singlestep", 2, 2, &arg0, &arg1))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { result = singlestep(x0, x1); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  pyresult = _cffi_from_c_int(result, long);
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  return pyresult;
}
#else
#  define _cffi_f_singlestep _cffi_d_singlestep
#endif

static int _cffi_d_step_until(struct global_state * x0, int x1, unsigned long x2, int x3)
{
  return step_until(x0, x1, x2, x3);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_step_until(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  unsigned long x2;
  int x3;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  int result;
  PyObject *pyresult;
  PyObject *arg0;
  PyObject *arg1;
  PyObject *arg2;
  PyObject *arg3;

  if (!PyArg_UnpackTuple(args, "step_until", 4, 4, &arg0, &arg1, &arg2, &arg3))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  x2 = _cffi_to_c_int(arg2, unsigned long);
  if (x2 == (unsigned long)-1 && PyErr_Occurred())
    return NULL;

  x3 = _cffi_to_c_int(arg3, int);
  if (x3 == (int)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { result = step_until(x0, x1, x2, x3); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  pyresult = _cffi_from_c_int(result, int);
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  return pyresult;
}
#else
#  define _cffi_f_step_until _cffi_d_step_until
#endif

static int _cffi_d_stepping_finish(struct global_state * x0, int x1, _Bool x2)
{
  return stepping_finish(x0, x1, x2);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_stepping_finish(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  _Bool x2;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  int result;
  PyObject *pyresult;
  PyObject *arg0;
  PyObject *arg1;
  PyObject *arg2;

  if (!PyArg_UnpackTuple(args, "stepping_finish", 3, 3, &arg0, &arg1, &arg2))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  x2 = (_Bool)_cffi_to_c__Bool(arg2);
  if (x2 == (_Bool)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { result = stepping_finish(x0, x1, x2); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  pyresult = _cffi_from_c_int(result, int);
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  return pyresult;
}
#else
#  define _cffi_f_stepping_finish _cffi_d_stepping_finish
#endif

static void _cffi_d_unregister_breakpoint(struct global_state * x0, unsigned long x1)
{
  unregister_breakpoint(x0, x1);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_unregister_breakpoint(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  unsigned long x1;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  PyObject *arg0;
  PyObject *arg1;

  if (!PyArg_UnpackTuple(args, "unregister_breakpoint", 2, 2, &arg0, &arg1))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, unsigned long);
  if (x1 == (unsigned long)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { unregister_breakpoint(x0, x1); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  Py_INCREF(Py_None);
  return Py_None;
}
#else
#  define _cffi_f_unregister_breakpoint _cffi_d_unregister_breakpoint
#endif

static void _cffi_d_unregister_hw_breakpoint(struct global_state * x0, int x1, unsigned long x2)
{
  unregister_hw_breakpoint(x0, x1, x2);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_unregister_hw_breakpoint(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  unsigned long x2;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  PyObject *arg0;
  PyObject *arg1;
  PyObject *arg2;

  if (!PyArg_UnpackTuple(args, "unregister_hw_breakpoint", 3, 3, &arg0, &arg1, &arg2))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  x2 = _cffi_to_c_int(arg2, unsigned long);
  if (x2 == (unsigned long)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { unregister_hw_breakpoint(x0, x1, x2); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  Py_INCREF(Py_None);
  return Py_None;
}
#else
#  define _cffi_f_unregister_hw_breakpoint _cffi_d_unregister_hw_breakpoint
#endif

static void _cffi_d_unregister_thread(struct global_state * x0, int x1)
{
  unregister_thread(x0, x1);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_unregister_thread(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  PyObject *arg0;
  PyObject *arg1;

  if (!PyArg_UnpackTuple(args, "unregister_thread", 2, 2, &arg0, &arg1))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { unregister_thread(x0, x1); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  Py_INCREF(Py_None);
  return Py_None;
}
#else
#  define _cffi_f_unregister_thread _cffi_d_unregister_thread
#endif

static struct thread_status * _cffi_d_wait_all_and_update_regs(struct global_state * x0, int x1)
{
  return wait_all_and_update_regs(x0, x1);
}
#ifndef PYPY_VERSION
static PyObject *
_cffi_f_wait_all_and_update_regs(PyObject *self, PyObject *args)
{
  struct global_state * x0;
  int x1;
  Py_ssize_t datasize;
  struct _cffi_freeme_s *large_args_free = NULL;
  struct thread_status * result;
  PyObject *pyresult;
  PyObject *arg0;
  PyObject *arg1;

  if (!PyArg_UnpackTuple(args, "wait_all_and_update_regs", 2, 2, &arg0, &arg1))
    return NULL;

  datasize = _cffi_prepare_pointer_call_argument(
      _cffi_type(4), arg0, (char **)&x0);
  if (datasize != 0) {
    x0 = ((size_t)datasize) <= 640 ? (struct global_state *)alloca((size_t)datasize) : NULL;
    if (_cffi_convert_array_argument(_cffi_type(4), arg0, (char **)&x0,
            datasize, &large_args_free) < 0)
      return NULL;
  }

  x1 = _cffi_to_c_int(arg1, int);
  if (x1 == (int)-1 && PyErr_Occurred())
    return NULL;

  Py_BEGIN_ALLOW_THREADS
  _cffi_restore_errno();
  { result = wait_all_and_update_regs(x0, x1); }
  _cffi_save_errno();
  Py_END_ALLOW_THREADS

  (void)self; /* unused */
  pyresult = _cffi_from_c_pointer((char *)result, _cffi_type(83));
  if (large_args_free != NULL) _cffi_free_array_arguments(large_args_free);
  return pyresult;
}
#else
#  define _cffi_f_wait_all_and_update_regs _cffi_d_wait_all_and_update_regs
#endif

_CFFI_UNUSED_FN
static void _cffi_checkfld_struct_fp_regs_struct(struct fp_regs_struct *p)
{
  /* only to generate compile-time warnings or errors */
  (void)p;
  (void)((p->type) | 0);  /* check that 'struct fp_regs_struct.type' is an integer */
  (void)((p->dirty) | 0);  /* check that 'struct fp_regs_struct.dirty' is an integer */
  (void)((p->fresh) | 0);  /* check that 'struct fp_regs_struct.fresh' is an integer */
  { unsigned char(*tmp)[6] = &p->bool_padding; (void)tmp; }
  { unsigned char(*tmp)[32] = &p->padding0; (void)tmp; }
  { struct reg_128(*tmp)[8] = &p->mmx; (void)tmp; }
  { struct reg_128(*tmp)[16] = &p->xmm0; (void)tmp; }
  { unsigned char(*tmp)[96] = &p->padding1; (void)tmp; }
  { unsigned char(*tmp)[64] = &p->padding2; (void)tmp; }
  { struct reg_128(*tmp)[16] = &p->ymm0; (void)tmp; }
  { unsigned char(*tmp)[128] = &p->padding3; (void)tmp; }
  { struct reg_128(*tmp)[4] = &p->bndregs; (void)tmp; }
  { struct reg_128 *tmp = &p->bndcfg; (void)tmp; }
  { unsigned char(*tmp)[48] = &p->padding4; (void)tmp; }
  { unsigned char(*tmp)[1600] = &p->padding6; (void)tmp; }
  (void)((p->pkru) | 0);  /* check that 'struct fp_regs_struct.pkru' is an integer */
  { unsigned char(*tmp)[60] = &p->padding7; (void)tmp; }
}
struct _cffi_align_struct_fp_regs_struct { char x; struct fp_regs_struct y; };

_CFFI_UNUSED_FN
static void _cffi_checkfld_struct_global_state(struct global_state *p)
{
  /* only to generate compile-time warnings or errors */
  (void)p;
  { struct thread * *tmp = &p->t_HEAD; (void)tmp; }
  { struct thread * *tmp = &p->dead_t_HEAD; (void)tmp; }
  { struct software_breakpoint * *tmp = &p->sw_b_HEAD; (void)tmp; }
  { struct hardware_breakpoint * *tmp = &p->hw_b_HEAD; (void)tmp; }
  (void)((p->handle_syscall_enabled) | 0);  /* check that 'struct global_state.handle_syscall_enabled' is an integer */
}
struct _cffi_align_struct_global_state { char x; struct global_state y; };

_CFFI_UNUSED_FN
static void _cffi_checkfld_struct_hardware_breakpoint(struct hardware_breakpoint *p)
{
  /* only to generate compile-time warnings or errors */
  (void)p;
  (void)((p->addr) | 0);  /* check that 'struct hardware_breakpoint.addr' is an integer */
  (void)((p->tid) | 0);  /* check that 'struct hardware_breakpoint.tid' is an integer */
  { char *tmp = &p->enabled; (void)tmp; }
  { char(*tmp)[2] = &p->type; (void)tmp; }
  { char *tmp = &p->len; (void)tmp; }
  { struct hardware_breakpoint * *tmp = &p->next; (void)tmp; }
}
struct _cffi_align_struct_hardware_breakpoint { char x; struct hardware_breakpoint y; };

_CFFI_UNUSED_FN
static void _cffi_checkfld_struct_ptrace_hit_bp(struct ptrace_hit_bp *p)
{
  /* only to generate compile-time warnings or errors */
  (void)p;
  (void)((p->pid) | 0);  /* check that 'struct ptrace_hit_bp.pid' is an integer */
  (void)((p->addr) | 0);  /* check that 'struct ptrace_hit_bp.addr' is an integer */
  (void)((p->bp_instruction) | 0);  /* check that 'struct ptrace_hit_bp.bp_instruction' is an integer */
  (void)((p->prev_instruction) | 0);  /* check that 'struct ptrace_hit_bp.prev_instruction' is an integer */
}
struct _cffi_align_struct_ptrace_hit_bp { char x; struct ptrace_hit_bp y; };

_CFFI_UNUSED_FN
static void _cffi_checkfld_struct_ptrace_regs_struct(struct ptrace_regs_struct *p)
{
  /* only to generate compile-time warnings or errors */
  (void)p;
  (void)((p->r15) | 0);  /* check that 'struct ptrace_regs_struct.r15' is an integer */
  (void)((p->r14) | 0);  /* check that 'struct ptrace_regs_struct.r14' is an integer */
  (void)((p->r13) | 0);  /* check that 'struct ptrace_regs_struct.r13' is an integer */
  (void)((p->r12) | 0);  /* check that 'struct ptrace_regs_struct.r12' is an integer */
  (void)((p->rbp) | 0);  /* check that 'struct ptrace_regs_struct.rbp' is an integer */
  (void)((p->rbx) | 0);  /* check that 'struct ptrace_regs_struct.rbx' is an integer */
  (void)((p->r11) | 0);  /* check that 'struct ptrace_regs_struct.r11' is an integer */
  (void)((p->r10) | 0);  /* check that 'struct ptrace_regs_struct.r10' is an integer */
  (void)((p->r9) | 0);  /* check that 'struct ptrace_regs_struct.r9' is an integer */
  (void)((p->r8) | 0);  /* check that 'struct ptrace_regs_struct.r8' is an integer */
  (void)((p->rax) | 0);  /* check that 'struct ptrace_regs_struct.rax' is an integer */
  (void)((p->rcx) | 0);  /* check that 'struct ptrace_regs_struct.rcx' is an integer */
  (void)((p->rdx) | 0);  /* check that 'struct ptrace_regs_struct.rdx' is an integer */
  (void)((p->rsi) | 0);  /* check that 'struct ptrace_regs_struct.rsi' is an integer */
  (void)((p->rdi) | 0);  /* check that 'struct ptrace_regs_struct.rdi' is an integer */
  (void)((p->orig_rax) | 0);  /* check that 'struct ptrace_regs_struct.orig_rax' is an integer */
  (void)((p->rip) | 0);  /* check that 'struct ptrace_regs_struct.rip' is an integer */
  (void)((p->cs) | 0);  /* check that 'struct ptrace_regs_struct.cs' is an integer */
  (void)((p->eflags) | 0);  /* check that 'struct ptrace_regs_struct.eflags' is an integer */
  (void)((p->rsp) | 0);  /* check that 'struct ptrace_regs_struct.rsp' is an integer */
  (void)((p->ss) | 0);  /* check that 'struct ptrace_regs_struct.ss' is an integer */
  (void)((p->fs_base) | 0);  /* check that 'struct ptrace_regs_struct.fs_base' is an integer */
  (void)((p->gs_base) | 0);  /* check that 'struct ptrace_regs_struct.gs_base' is an integer */
  (void)((p->ds) | 0);  /* check that 'struct ptrace_regs_struct.ds' is an integer */
  (void)((p->es) | 0);  /* check that 'struct ptrace_regs_struct.es' is an integer */
  (void)((p->fs) | 0);  /* check that 'struct ptrace_regs_struct.fs' is an integer */
  (void)((p->gs) | 0);  /* check that 'struct ptrace_regs_struct.gs' is an integer */
}
struct _cffi_align_struct_ptrace_regs_struct { char x; struct ptrace_regs_struct y; };

_CFFI_UNUSED_FN
static void _cffi_checkfld_struct_reg_128(struct reg_128 *p)
{
  /* only to generate compile-time warnings or errors */
  (void)p;
  { unsigned char(*tmp)[16] = &p->data; (void)tmp; }
}
struct _cffi_align_struct_reg_128 { char x; struct reg_128 y; };

_CFFI_UNUSED_FN
static void _cffi_checkfld_struct_software_breakpoint(struct software_breakpoint *p)
{
  /* only to generate compile-time warnings or errors */
  (void)p;
  (void)((p->addr) | 0);  /* check that 'struct software_breakpoint.addr' is an integer */
  (void)((p->instruction) | 0);  /* check that 'struct software_breakpoint.instruction' is an integer */
  (void)((p->patched_instruction) | 0);  /* check that 'struct software_breakpoint.patched_instruction' is an integer */
  { char *tmp = &p->enabled; (void)tmp; }
  { struct software_breakpoint * *tmp = &p->next; (void)tmp; }
}
struct _cffi_align_struct_software_breakpoint { char x; struct software_breakpoint y; };

_CFFI_UNUSED_FN
static void _cffi_checkfld_struct_thread(struct thread *p)
{
  /* only to generate compile-time warnings or errors */
  (void)p;
  (void)((p->tid) | 0);  /* check that 'struct thread.tid' is an integer */
  { struct ptrace_regs_struct *tmp = &p->regs; (void)tmp; }
  { struct fp_regs_struct *tmp = &p->fpregs; (void)tmp; }
  (void)((p->signal_to_forward) | 0);  /* check that 'struct thread.signal_to_forward' is an integer */
  { struct thread * *tmp = &p->next; (void)tmp; }
}
struct _cffi_align_struct_thread { char x; struct thread y; };

_CFFI_UNUSED_FN
static void _cffi_checkfld_struct_thread_status(struct thread_status *p)
{
  /* only to generate compile-time warnings or errors */
  (void)p;
  (void)((p->tid) | 0);  /* check that 'struct thread_status.tid' is an integer */
  (void)((p->status) | 0);  /* check that 'struct thread_status.status' is an integer */
  { struct thread_status * *tmp = &p->next; (void)tmp; }
}
struct _cffi_align_struct_thread_status { char x; struct thread_status y; };

static const struct _cffi_global_s _cffi_globals[] = {
  { "cont_all_and_set_bps", (void *)_cffi_f_cont_all_and_set_bps, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 3), (void *)_cffi_d_cont_all_and_set_bps },
  { "disable_breakpoint", (void *)_cffi_f_disable_breakpoint, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 78), (void *)_cffi_d_disable_breakpoint },
  { "disable_hw_breakpoint", (void *)_cffi_f_disable_hw_breakpoint, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 66), (void *)_cffi_d_disable_hw_breakpoint },
  { "enable_breakpoint", (void *)_cffi_f_enable_breakpoint, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 78), (void *)_cffi_d_enable_breakpoint },
  { "enable_hw_breakpoint", (void *)_cffi_f_enable_hw_breakpoint, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 66), (void *)_cffi_d_enable_hw_breakpoint },
  { "free_breakpoints", (void *)_cffi_f_free_breakpoints, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_O, 59), (void *)_cffi_d_free_breakpoints },
  { "free_thread_list", (void *)_cffi_f_free_thread_list, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_O, 59), (void *)_cffi_d_free_thread_list },
  { "free_thread_status_list", (void *)_cffi_f_free_thread_status_list, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_O, 82), (void *)_cffi_d_free_thread_status_list },
  { "get_fp_regs", (void *)_cffi_f_get_fp_regs, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 55), (void *)_cffi_d_get_fp_regs },
  { "get_hit_hw_breakpoint", (void *)_cffi_f_get_hit_hw_breakpoint, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 48), (void *)_cffi_d_get_hit_hw_breakpoint },
  { "get_remaining_hw_breakpoint_count", (void *)_cffi_f_get_remaining_hw_breakpoint_count, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 3), (void *)_cffi_d_get_remaining_hw_breakpoint_count },
  { "get_remaining_hw_watchpoint_count", (void *)_cffi_f_get_remaining_hw_watchpoint_count, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 3), (void *)_cffi_d_get_remaining_hw_watchpoint_count },
  { "get_thread_fp_regs", (void *)_cffi_f_get_thread_fp_regs, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 24), (void *)_cffi_d_get_thread_fp_regs },
  { "ptrace_attach", (void *)_cffi_f_ptrace_attach, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_O, 0), (void *)_cffi_d_ptrace_attach },
  { "ptrace_detach_and_cont", (void *)_cffi_f_ptrace_detach_and_cont, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 62), (void *)_cffi_d_ptrace_detach_and_cont },
  { "ptrace_detach_for_kill", (void *)_cffi_f_ptrace_detach_for_kill, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 62), (void *)_cffi_d_ptrace_detach_for_kill },
  { "ptrace_detach_for_migration", (void *)_cffi_f_ptrace_detach_for_migration, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 62), (void *)_cffi_d_ptrace_detach_for_migration },
  { "ptrace_geteventmsg", (void *)_cffi_f_ptrace_geteventmsg, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_O, 36), (void *)_cffi_d_ptrace_geteventmsg },
  { "ptrace_peekdata", (void *)_cffi_f_ptrace_peekdata, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 39), (void *)_cffi_d_ptrace_peekdata },
  { "ptrace_pokedata", (void *)_cffi_f_ptrace_pokedata, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 43), (void *)_cffi_d_ptrace_pokedata },
  { "ptrace_reattach_from_gdb", (void *)_cffi_f_ptrace_reattach_from_gdb, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 62), (void *)_cffi_d_ptrace_reattach_from_gdb },
  { "ptrace_set_options", (void *)_cffi_f_ptrace_set_options, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_O, 52), (void *)_cffi_d_ptrace_set_options },
  { "ptrace_trace_me", (void *)_cffi_f_ptrace_trace_me, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_N, 18), (void *)_cffi_d_ptrace_trace_me },
  { "register_breakpoint", (void *)_cffi_f_register_breakpoint, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 66), (void *)_cffi_d_register_breakpoint },
  { "register_hw_breakpoint", (void *)_cffi_f_register_hw_breakpoint, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 71), (void *)_cffi_d_register_hw_breakpoint },
  { "register_thread", (void *)_cffi_f_register_thread, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 28), (void *)_cffi_d_register_thread },
  { "set_fp_regs", (void *)_cffi_f_set_fp_regs, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 55), (void *)_cffi_d_set_fp_regs },
  { "singlestep", (void *)_cffi_f_singlestep, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 20), (void *)_cffi_d_singlestep },
  { "step_until", (void *)_cffi_f_step_until, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 12), (void *)_cffi_d_step_until },
  { "stepping_finish", (void *)_cffi_f_stepping_finish, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 7), (void *)_cffi_d_stepping_finish },
  { "unregister_breakpoint", (void *)_cffi_f_unregister_breakpoint, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 78), (void *)_cffi_d_unregister_breakpoint },
  { "unregister_hw_breakpoint", (void *)_cffi_f_unregister_hw_breakpoint, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 66), (void *)_cffi_d_unregister_hw_breakpoint },
  { "unregister_thread", (void *)_cffi_f_unregister_thread, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 62), (void *)_cffi_d_unregister_thread },
  { "wait_all_and_update_regs", (void *)_cffi_f_wait_all_and_update_regs, _CFFI_OP(_CFFI_OP_CPYTHON_BLTN_V, 32), (void *)_cffi_d_wait_all_and_update_regs },
};

static const struct _cffi_field_s _cffi_fields[] = {
  { "type", offsetof(struct fp_regs_struct, type),
            sizeof(((struct fp_regs_struct *)0)->type),
            _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "dirty", offsetof(struct fp_regs_struct, dirty),
             sizeof(((struct fp_regs_struct *)0)->dirty),
             _CFFI_OP(_CFFI_OP_NOOP, 10) },
  { "fresh", offsetof(struct fp_regs_struct, fresh),
             sizeof(((struct fp_regs_struct *)0)->fresh),
             _CFFI_OP(_CFFI_OP_NOOP, 10) },
  { "bool_padding", offsetof(struct fp_regs_struct, bool_padding),
                    sizeof(((struct fp_regs_struct *)0)->bool_padding),
                    _CFFI_OP(_CFFI_OP_NOOP, 122) },
  { "padding0", offsetof(struct fp_regs_struct, padding0),
                sizeof(((struct fp_regs_struct *)0)->padding0),
                _CFFI_OP(_CFFI_OP_NOOP, 114) },
  { "mmx", offsetof(struct fp_regs_struct, mmx),
           sizeof(((struct fp_regs_struct *)0)->mmx),
           _CFFI_OP(_CFFI_OP_NOOP, 100) },
  { "xmm0", offsetof(struct fp_regs_struct, xmm0),
            sizeof(((struct fp_regs_struct *)0)->xmm0),
            _CFFI_OP(_CFFI_OP_NOOP, 96) },
  { "padding1", offsetof(struct fp_regs_struct, padding1),
                sizeof(((struct fp_regs_struct *)0)->padding1),
                _CFFI_OP(_CFFI_OP_NOOP, 124) },
  { "padding2", offsetof(struct fp_regs_struct, padding2),
                sizeof(((struct fp_regs_struct *)0)->padding2),
                _CFFI_OP(_CFFI_OP_NOOP, 120) },
  { "ymm0", offsetof(struct fp_regs_struct, ymm0),
            sizeof(((struct fp_regs_struct *)0)->ymm0),
            _CFFI_OP(_CFFI_OP_NOOP, 96) },
  { "padding3", offsetof(struct fp_regs_struct, padding3),
                sizeof(((struct fp_regs_struct *)0)->padding3),
                _CFFI_OP(_CFFI_OP_NOOP, 108) },
  { "bndregs", offsetof(struct fp_regs_struct, bndregs),
               sizeof(((struct fp_regs_struct *)0)->bndregs),
               _CFFI_OP(_CFFI_OP_NOOP, 98) },
  { "bndcfg", offsetof(struct fp_regs_struct, bndcfg),
              sizeof(((struct fp_regs_struct *)0)->bndcfg),
              _CFFI_OP(_CFFI_OP_NOOP, 95) },
  { "padding4", offsetof(struct fp_regs_struct, padding4),
                sizeof(((struct fp_regs_struct *)0)->padding4),
                _CFFI_OP(_CFFI_OP_NOOP, 116) },
  { "padding6", offsetof(struct fp_regs_struct, padding6),
                sizeof(((struct fp_regs_struct *)0)->padding6),
                _CFFI_OP(_CFFI_OP_NOOP, 110) },
  { "pkru", offsetof(struct fp_regs_struct, pkru),
            sizeof(((struct fp_regs_struct *)0)->pkru),
            _CFFI_OP(_CFFI_OP_NOOP, 126) },
  { "padding7", offsetof(struct fp_regs_struct, padding7),
                sizeof(((struct fp_regs_struct *)0)->padding7),
                _CFFI_OP(_CFFI_OP_NOOP, 118) },
  { "t_HEAD", offsetof(struct global_state, t_HEAD),
              sizeof(((struct global_state *)0)->t_HEAD),
              _CFFI_OP(_CFFI_OP_NOOP, 104) },
  { "dead_t_HEAD", offsetof(struct global_state, dead_t_HEAD),
                   sizeof(((struct global_state *)0)->dead_t_HEAD),
                   _CFFI_OP(_CFFI_OP_NOOP, 104) },
  { "sw_b_HEAD", offsetof(struct global_state, sw_b_HEAD),
                 sizeof(((struct global_state *)0)->sw_b_HEAD),
                 _CFFI_OP(_CFFI_OP_NOOP, 102) },
  { "hw_b_HEAD", offsetof(struct global_state, hw_b_HEAD),
                 sizeof(((struct global_state *)0)->hw_b_HEAD),
                 _CFFI_OP(_CFFI_OP_NOOP, 90) },
  { "handle_syscall_enabled", offsetof(struct global_state, handle_syscall_enabled),
                              sizeof(((struct global_state *)0)->handle_syscall_enabled),
                              _CFFI_OP(_CFFI_OP_NOOP, 10) },
  { "addr", offsetof(struct hardware_breakpoint, addr),
            sizeof(((struct hardware_breakpoint *)0)->addr),
            _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "tid", offsetof(struct hardware_breakpoint, tid),
           sizeof(((struct hardware_breakpoint *)0)->tid),
           _CFFI_OP(_CFFI_OP_NOOP, 1) },
  { "enabled", offsetof(struct hardware_breakpoint, enabled),
               sizeof(((struct hardware_breakpoint *)0)->enabled),
               _CFFI_OP(_CFFI_OP_NOOP, 76) },
  { "type", offsetof(struct hardware_breakpoint, type),
            sizeof(((struct hardware_breakpoint *)0)->type),
            _CFFI_OP(_CFFI_OP_NOOP, 85) },
  { "len", offsetof(struct hardware_breakpoint, len),
           sizeof(((struct hardware_breakpoint *)0)->len),
           _CFFI_OP(_CFFI_OP_NOOP, 76) },
  { "next", offsetof(struct hardware_breakpoint, next),
            sizeof(((struct hardware_breakpoint *)0)->next),
            _CFFI_OP(_CFFI_OP_NOOP, 90) },
  { "pid", offsetof(struct ptrace_hit_bp, pid),
           sizeof(((struct ptrace_hit_bp *)0)->pid),
           _CFFI_OP(_CFFI_OP_NOOP, 1) },
  { "addr", offsetof(struct ptrace_hit_bp, addr),
            sizeof(((struct ptrace_hit_bp *)0)->addr),
            _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "bp_instruction", offsetof(struct ptrace_hit_bp, bp_instruction),
                      sizeof(((struct ptrace_hit_bp *)0)->bp_instruction),
                      _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "prev_instruction", offsetof(struct ptrace_hit_bp, prev_instruction),
                        sizeof(((struct ptrace_hit_bp *)0)->prev_instruction),
                        _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "r15", offsetof(struct ptrace_regs_struct, r15),
           sizeof(((struct ptrace_regs_struct *)0)->r15),
           _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "r14", offsetof(struct ptrace_regs_struct, r14),
           sizeof(((struct ptrace_regs_struct *)0)->r14),
           _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "r13", offsetof(struct ptrace_regs_struct, r13),
           sizeof(((struct ptrace_regs_struct *)0)->r13),
           _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "r12", offsetof(struct ptrace_regs_struct, r12),
           sizeof(((struct ptrace_regs_struct *)0)->r12),
           _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "rbp", offsetof(struct ptrace_regs_struct, rbp),
           sizeof(((struct ptrace_regs_struct *)0)->rbp),
           _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "rbx", offsetof(struct ptrace_regs_struct, rbx),
           sizeof(((struct ptrace_regs_struct *)0)->rbx),
           _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "r11", offsetof(struct ptrace_regs_struct, r11),
           sizeof(((struct ptrace_regs_struct *)0)->r11),
           _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "r10", offsetof(struct ptrace_regs_struct, r10),
           sizeof(((struct ptrace_regs_struct *)0)->r10),
           _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "r9", offsetof(struct ptrace_regs_struct, r9),
          sizeof(((struct ptrace_regs_struct *)0)->r9),
          _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "r8", offsetof(struct ptrace_regs_struct, r8),
          sizeof(((struct ptrace_regs_struct *)0)->r8),
          _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "rax", offsetof(struct ptrace_regs_struct, rax),
           sizeof(((struct ptrace_regs_struct *)0)->rax),
           _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "rcx", offsetof(struct ptrace_regs_struct, rcx),
           sizeof(((struct ptrace_regs_struct *)0)->rcx),
           _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "rdx", offsetof(struct ptrace_regs_struct, rdx),
           sizeof(((struct ptrace_regs_struct *)0)->rdx),
           _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "rsi", offsetof(struct ptrace_regs_struct, rsi),
           sizeof(((struct ptrace_regs_struct *)0)->rsi),
           _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "rdi", offsetof(struct ptrace_regs_struct, rdi),
           sizeof(((struct ptrace_regs_struct *)0)->rdi),
           _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "orig_rax", offsetof(struct ptrace_regs_struct, orig_rax),
                sizeof(((struct ptrace_regs_struct *)0)->orig_rax),
                _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "rip", offsetof(struct ptrace_regs_struct, rip),
           sizeof(((struct ptrace_regs_struct *)0)->rip),
           _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "cs", offsetof(struct ptrace_regs_struct, cs),
          sizeof(((struct ptrace_regs_struct *)0)->cs),
          _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "eflags", offsetof(struct ptrace_regs_struct, eflags),
              sizeof(((struct ptrace_regs_struct *)0)->eflags),
              _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "rsp", offsetof(struct ptrace_regs_struct, rsp),
           sizeof(((struct ptrace_regs_struct *)0)->rsp),
           _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "ss", offsetof(struct ptrace_regs_struct, ss),
          sizeof(((struct ptrace_regs_struct *)0)->ss),
          _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "fs_base", offsetof(struct ptrace_regs_struct, fs_base),
               sizeof(((struct ptrace_regs_struct *)0)->fs_base),
               _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "gs_base", offsetof(struct ptrace_regs_struct, gs_base),
               sizeof(((struct ptrace_regs_struct *)0)->gs_base),
               _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "ds", offsetof(struct ptrace_regs_struct, ds),
          sizeof(((struct ptrace_regs_struct *)0)->ds),
          _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "es", offsetof(struct ptrace_regs_struct, es),
          sizeof(((struct ptrace_regs_struct *)0)->es),
          _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "fs", offsetof(struct ptrace_regs_struct, fs),
          sizeof(((struct ptrace_regs_struct *)0)->fs),
          _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "gs", offsetof(struct ptrace_regs_struct, gs),
          sizeof(((struct ptrace_regs_struct *)0)->gs),
          _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "data", offsetof(struct reg_128, data),
            sizeof(((struct reg_128 *)0)->data),
            _CFFI_OP(_CFFI_OP_NOOP, 112) },
  { "addr", offsetof(struct software_breakpoint, addr),
            sizeof(((struct software_breakpoint *)0)->addr),
            _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "instruction", offsetof(struct software_breakpoint, instruction),
                   sizeof(((struct software_breakpoint *)0)->instruction),
                   _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "patched_instruction", offsetof(struct software_breakpoint, patched_instruction),
                           sizeof(((struct software_breakpoint *)0)->patched_instruction),
                           _CFFI_OP(_CFFI_OP_NOOP, 15) },
  { "enabled", offsetof(struct software_breakpoint, enabled),
               sizeof(((struct software_breakpoint *)0)->enabled),
               _CFFI_OP(_CFFI_OP_NOOP, 76) },
  { "next", offsetof(struct software_breakpoint, next),
            sizeof(((struct software_breakpoint *)0)->next),
            _CFFI_OP(_CFFI_OP_NOOP, 102) },
  { "tid", offsetof(struct thread, tid),
           sizeof(((struct thread *)0)->tid),
           _CFFI_OP(_CFFI_OP_NOOP, 1) },
  { "regs", offsetof(struct thread, regs),
            sizeof(((struct thread *)0)->regs),
            _CFFI_OP(_CFFI_OP_NOOP, 94) },
  { "fpregs", offsetof(struct thread, fpregs),
              sizeof(((struct thread *)0)->fpregs),
              _CFFI_OP(_CFFI_OP_NOOP, 88) },
  { "signal_to_forward", offsetof(struct thread, signal_to_forward),
                         sizeof(((struct thread *)0)->signal_to_forward),
                         _CFFI_OP(_CFFI_OP_NOOP, 1) },
  { "next", offsetof(struct thread, next),
            sizeof(((struct thread *)0)->next),
            _CFFI_OP(_CFFI_OP_NOOP, 104) },
  { "tid", offsetof(struct thread_status, tid),
           sizeof(((struct thread_status *)0)->tid),
           _CFFI_OP(_CFFI_OP_NOOP, 1) },
  { "status", offsetof(struct thread_status, status),
              sizeof(((struct thread_status *)0)->status),
              _CFFI_OP(_CFFI_OP_NOOP, 1) },
  { "next", offsetof(struct thread_status, next),
            sizeof(((struct thread_status *)0)->next),
            _CFFI_OP(_CFFI_OP_NOOP, 83) },
};

static const struct _cffi_struct_union_s _cffi_struct_unions[] = {
  { "fp_regs_struct", 88, _CFFI_F_CHECK_FIELDS|_CFFI_F_PACKED,
    sizeof(struct fp_regs_struct), offsetof(struct _cffi_align_struct_fp_regs_struct, y), 0, 17 },
  { "global_state", 89, _CFFI_F_CHECK_FIELDS,
    sizeof(struct global_state), offsetof(struct _cffi_align_struct_global_state, y), 17, 5 },
  { "hardware_breakpoint", 91, _CFFI_F_CHECK_FIELDS,
    sizeof(struct hardware_breakpoint), offsetof(struct _cffi_align_struct_hardware_breakpoint, y), 22, 6 },
  { "ptrace_hit_bp", 92, _CFFI_F_CHECK_FIELDS,
    sizeof(struct ptrace_hit_bp), offsetof(struct _cffi_align_struct_ptrace_hit_bp, y), 28, 4 },
  { "ptrace_regs_struct", 94, _CFFI_F_CHECK_FIELDS,
    sizeof(struct ptrace_regs_struct), offsetof(struct _cffi_align_struct_ptrace_regs_struct, y), 32, 27 },
  { "reg_128", 95, _CFFI_F_CHECK_FIELDS|_CFFI_F_PACKED,
    sizeof(struct reg_128), offsetof(struct _cffi_align_struct_reg_128, y), 59, 1 },
  { "software_breakpoint", 103, _CFFI_F_CHECK_FIELDS,
    sizeof(struct software_breakpoint), offsetof(struct _cffi_align_struct_software_breakpoint, y), 60, 5 },
  { "thread", 105, _CFFI_F_CHECK_FIELDS,
    sizeof(struct thread), offsetof(struct _cffi_align_struct_thread, y), 65, 5 },
  { "thread_status", 106, _CFFI_F_CHECK_FIELDS,
    sizeof(struct thread_status), offsetof(struct _cffi_align_struct_thread_status, y), 70, 3 },
};

static const struct _cffi_type_context_s _cffi_type_context = {
  _cffi_types,
  _cffi_globals,
  _cffi_fields,
  _cffi_struct_unions,
  NULL,  /* no enums */
  NULL,  /* no typenames */
  34,  /* num_globals */
  9,  /* num_struct_unions */
  0,  /* num_enums */
  0,  /* num_typenames */
  NULL,  /* no includes */
  128,  /* num_types */
  0,  /* flags */
};

#ifdef __GNUC__
#  pragma GCC visibility push(default)  /* for -fvisibility= */
#endif

#ifdef PYPY_VERSION
PyMODINIT_FUNC
_cffi_pypyinit__ptrace_cffi(const void *p[])
{
    p[0] = (const void *)0x2601;
    p[1] = &_cffi_type_context;
#if PY_MAJOR_VERSION >= 3
    return NULL;
#endif
}
#  ifdef _MSC_VER
     PyMODINIT_FUNC
#  if PY_MAJOR_VERSION >= 3
     PyInit__ptrace_cffi(void) { return NULL; }
#  else
     init_ptrace_cffi(void) { }
#  endif
#  endif
#elif PY_MAJOR_VERSION >= 3
PyMODINIT_FUNC
PyInit__ptrace_cffi(void)
{
  return _cffi_init("libdebug.cffi._ptrace_cffi", 0x2601, &_cffi_type_context);
}
#else
PyMODINIT_FUNC
init_ptrace_cffi(void)
{
  _cffi_init("libdebug.cffi._ptrace_cffi", 0x2601, &_cffi_type_context);
}
#endif

#ifdef __GNUC__
#  pragma GCC visibility pop
#endif
