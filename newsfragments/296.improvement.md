Added two new pipe interaction functions: `match_recvuntil` and `match_recverruntil`. These functions allow matching patterns in `stdout` or `stderr` while reading until a specified pattern is matched.