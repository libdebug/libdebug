- Debug processes in local Docker or Podman containers with `DockerDebugger` and `DockerDebuggerMixin`.
- Resolve container symbols through a local file cache, preserve debugger plugins, and clean up container launches on exit.
